---
phase: 08.1-content-and-ui-optimization-for-keyword-ranking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/analytics/build-keyword-priority-map.mjs
  - scripts/seo/check-template-content-contract.mjs
  - scripts/seo/check-messaging-guardrails.mjs
  - package.json
  - lib/messaging.ts
  - docs/seo/keyword-optimization-priority-map.md
  - docs/seo/content-ui-optimization-guardrails.md
autonomous: true
requirements:
  - SEO-06
  - SEO-09
must_haves:
  truths:
    - "Content/UI refresh work is prioritized by deterministic opportunity output, not ad-hoc page edits."
    - "Messaging and template-copy guardrails prevent ICP/promise drift during rewrite work."
    - "Optimization checks are runnable locally and in CI before any redesign plan merges."
  artifacts:
    - path: "scripts/analytics/build-keyword-priority-map.mjs"
      provides: "Deterministic priority-map generator using weekly SEO report artifacts."
    - path: "scripts/seo/check-template-content-contract.mjs"
      provides: "Template-level copy/intent contract validation for refreshed SEO page types."
    - path: "docs/seo/keyword-optimization-priority-map.md"
      provides: "Documented first-wave target pages with action type, rationale, and ownership fields."
    - path: "docs/seo/content-ui-optimization-guardrails.md"
      provides: "Runbook for copy/UI contract checks and release criteria."
  key_links:
    - from: "scripts/analytics/build-keyword-priority-map.mjs"
      to: "docs/seo/keyword-optimization-priority-map.md"
      via: "priority output fields are documented and used as the execution queue for refresh plans"
      pattern: "action_type|owner|status|rationale|template_type"
    - from: "scripts/seo/check-template-content-contract.mjs"
      to: "app/features/page.tsx + app/industries/page.tsx + app/compare/page.tsx + app/contact/page.tsx"
      via: "guardrail checks enforce required copy blocks, headings, and CTA presence on target templates"
      pattern: "required|headline|cta|template"
---

<objective>
Create a deterministic optimization foundation before template rewrites begin.

Purpose: Ensure Phase 08.1 execution is ranked, measurable, and guarded by repeatable copy/UI quality checks.
Output: Priority-map builder, template-content guardrail script, and documented optimization contract.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08.1-content-and-ui-optimization-for-keyword-ranking/08.1-RESEARCH.md
@.planning/phases/06-qa-automation-and-scale-operations/.tmp-weekly-report/weekly-seo-ops-report.json
@scripts/seo/check-messaging-guardrails.mjs
@scripts/seo/check-seo-contract.mjs
@scripts/seo/check-internal-links.mjs
@lib/messaging.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic keyword/content priority-map artifact</name>
  <files>scripts/analytics/build-keyword-priority-map.mjs, docs/seo/keyword-optimization-priority-map.md</files>
  <behavior>
    - Weekly SEO report input is transformed into a ranked queue of pages to refresh.
    - Output includes explicit `action_type`, `owner`, `status`, and rationale fields per row.
    - Priority-map format is stable so Plan 02/03 can consume it without reinterpretation.
  </behavior>
  <action>Create `build-keyword-priority-map.mjs` to parse weekly report JSON/CSV and emit a deterministic ranked map with template type, opportunity score, action type (`new`, `update`, `link-fix`, `cta-test`), owner, status, and rationale. Document the first optimization wave in `keyword-optimization-priority-map.md` with direct links to target routes.</action>
  <verify>
    <automated>node scripts/analytics/build-keyword-priority-map.mjs --input .planning/phases/06-qa-automation-and-scale-operations/.tmp-weekly-report/weekly-seo-ops-report.json --output .planning/phases/08.1-content-and-ui-optimization-for-keyword-ranking/.tmp-priority-map.json && rg --line-number "action_type|owner|status|rationale|template_type" .planning/phases/08.1-content-and-ui-optimization-for-keyword-ranking/.tmp-priority-map.json docs/seo/keyword-optimization-priority-map.md</automated>
  </verify>
  <done>Optimization work is prioritized with deterministic fields and documented first-wave targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand messaging primitives for intent-led copy refreshes</name>
  <files>lib/messaging.ts, scripts/seo/check-messaging-guardrails.mjs</files>
  <behavior>
    - Shared messaging module exposes reusable headline/body/CTA variants for refreshed templates.
    - Messaging guardrail script validates new required phrases/sections across target page files.
    - Existing ICP constraints and promise-order guardrails remain intact.
  </behavior>
  <action>Extend `lib/messaging.ts` with template-safe copy primitives for ranking-oriented rewrites (intent-led headline options, proof-led body helpers, CTA framing variants). Update `check-messaging-guardrails.mjs` to verify refreshed templates consume these helpers and do not reintroduce disallowed qualifier language.</action>
  <verify>
    <automated>npm run seo:check-messaging</automated>
  </verify>
  <done>Copy rewrites have one shared source of truth with enforced guardrails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add template-content contract checker and runbook</name>
  <files>scripts/seo/check-template-content-contract.mjs, docs/seo/content-ui-optimization-guardrails.md, package.json</files>
  <behavior>
    - New checker validates required structural/copy elements for refreshed templates.
    - Package scripts expose a single command to run this checker in local and CI contexts.
    - Runbook documents pass/fail criteria and remediation flow.
  </behavior>
  <action>Create `check-template-content-contract.mjs` to enforce required heading blocks, CTA presence, and key internal-link sections on home/hub/detail templates. Add script alias in `package.json` and document usage, expected diagnostics, and release-gate criteria in `content-ui-optimization-guardrails.md`.</action>
  <verify>
    <automated>node scripts/seo/check-template-content-contract.mjs</automated>
  </verify>
  <done>Template copy/UI contract checks are deterministic, documented, and callable through package scripts.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/analytics/build-keyword-priority-map.mjs --input .planning/phases/06-qa-automation-and-scale-operations/.tmp-weekly-report/weekly-seo-ops-report.json --output .planning/phases/08.1-content-and-ui-optimization-for-keyword-ranking/.tmp-priority-map.json`
- [ ] `npm run seo:check-messaging`
- [ ] `node scripts/seo/check-template-content-contract.mjs`
</verification>

<success_criteria>
- A deterministic, owner-ready page optimization backlog exists for immediate execution.
- Messaging guardrails cover refreshed template surfaces without breaking existing ICP constraints.
- Template-content contract checks run locally/CI and block structurally incomplete refreshes.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-content-and-ui-optimization-for-keyword-ranking/08.1-01-SUMMARY.md`
</output>

