---
phase: 01-seo-and-positioning-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - lib/schema.ts
  - components/FAQSection.tsx
  - app/layout.tsx
  - app/page.tsx
  - app/blog/[slug]/page.tsx
  - app/guides/[slug]/page.tsx
  - app/case-studies/[slug]/page.tsx
  - app/features/[slug]/page.tsx
  - app/industries/[slug]/page.tsx
  - scripts/seo/check-schema-presence.mjs
  - lib/__tests__/schema.test.ts
  - docs/seo/schema-matrix.md
  - package.json
autonomous: true
requirements:
  - SEO-04
must_haves:
  truths:
    - "Each core template emits required JSON-LD for its page type (Article/FAQ/Breadcrumb plus Organization/WebSite where applicable)."
    - "Schema output is generated by typed builders instead of copy-pasted inline objects."
    - "A schema presence check fails when required template schema is missing."
  artifacts:
    - path: "lib/schema.ts"
      provides: "Typed schema builders for Organization, WebSite, FAQPage, BreadcrumbList, and Article patterns."
    - path: "docs/seo/schema-matrix.md"
      provides: "Template-to-schema requirement mapping used by checks."
    - path: "scripts/seo/check-schema-presence.mjs"
      provides: "Automated guardrail for required schema coverage."
    - path: "lib/__tests__/schema.test.ts"
      provides: "Unit coverage for schema builder output shape and required fields."
  key_links:
    - from: "app/blog/[slug]/page.tsx"
      to: "lib/schema.ts"
      via: "injects Article and Breadcrumb schema via shared builder"
      pattern: "from \"@/lib/schema\""
    - from: "app/page.tsx"
      to: "components/FAQSection.tsx"
      via: "uses shared FAQ source to emit FAQ schema"
    - from: "scripts/seo/check-schema-presence.mjs"
      to: "docs/seo/schema-matrix.md"
      via: "validates coverage against documented template matrix"
---

<objective>
Implement the schema baseline and verification checks for current SEO templates.

Purpose: Satisfy required structured-data coverage with reusable builders and enforceable checks.
Output: Shared schema utilities, template-level JSON-LD integration, and schema coverage gates.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-seo-and-positioning-foundation/01-CONTEXT.md
@.planning/phases/01-seo-and-positioning-foundation/01-RESEARCH.md
@.planning/phases/01-seo-and-positioning-foundation/01-01-PLAN.md
@app/layout.tsx
@components/FAQSection.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build typed schema utilities and reusable data sources</name>
  <files>lib/schema.ts, components/FAQSection.tsx</files>
  <behavior>
    - Schema builders return valid JSON-LD objects with predictable required fields.
    - FAQ content can be consumed by both UI rendering and FAQ schema generation.
  </behavior>
  <action>Create `lib/schema.ts` with typed builders for Organization, WebSite, FAQPage, BreadcrumbList, and Article-style content. Refactor `components/FAQSection.tsx` to export FAQ data in a way that preserves current UI behavior while enabling structured-data reuse on the homepage. Keep API surface simple and aligned with existing route data modules.</action>
  <verify>
    <automated>npm run lint -- lib/schema.ts components/FAQSection.tsx</automated>
  </verify>
  <done>Typed schema builders and reusable FAQ data exist and compile cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire schema builders into required template pages</name>
  <files>app/layout.tsx, app/page.tsx, app/blog/[slug]/page.tsx, app/guides/[slug]/page.tsx, app/case-studies/[slug]/page.tsx, app/features/[slug]/page.tsx, app/industries/[slug]/page.tsx</files>
  <behavior>
    - Required template types emit matching schema via shared utilities.
    - Existing page rendering and metadata behavior remain intact.
  </behavior>
  <action>Integrate schema builders into core templates: keep Organization/WebSite in layout, add FAQ schema to homepage, and add Article/Breadcrumb schema to long-form and slug detail templates where applicable. Avoid duplicate inline schema blocks by importing shared builders. Preserve canonical/page metadata behavior from Plan 01-01.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build succeeds and core templates emit JSON-LD from shared utilities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add schema matrix and automated coverage checks</name>
  <files>docs/seo/schema-matrix.md, scripts/seo/check-schema-presence.mjs, lib/__tests__/schema.test.ts, package.json</files>
  <behavior>
    - Required schema/template matrix is documented and machine-checkable.
    - Failing/missing schema coverage causes automated check failure.
  </behavior>
  <action>Create `docs/seo/schema-matrix.md` that maps each template class to required schema types. Implement `scripts/seo/check-schema-presence.mjs` to enforce the matrix against route source files and shared schema builder usage. Add builder-level unit tests in `lib/__tests__/schema.test.ts` and expose a package command for schema checks.</action>
  <verify>
    <automated>npx vitest run lib/__tests__/schema.test.ts && node scripts/seo/check-schema-presence.mjs</automated>
  </verify>
  <done>Schema matrix exists, tests pass, and automated schema coverage check succeeds.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `npx vitest run lib/__tests__/schema.test.ts`
- [ ] `node scripts/seo/check-schema-presence.mjs`
</verification>

<success_criteria>
- Required schema is present on all core template types.
- Schema generation is centralized in typed utilities.
- Missing schema regressions are caught automatically by CI-friendly commands.
</success_criteria>

<output>
After completion, create `.planning/phases/01-seo-and-positioning-foundation/01-02-SUMMARY.md`
</output>
