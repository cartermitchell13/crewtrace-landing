---
phase: 01-seo-and-positioning-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/seo.ts
  - lib/seoPolicy.ts
  - app/robots.ts
  - app/sitemap.ts
  - app/page.tsx
  - app/about/page.tsx
  - app/blog/page.tsx
  - app/case-studies/page.tsx
  - app/guides/page.tsx
  - app/industries/page.tsx
  - app/contact/layout.tsx
  - app/calculator/layout.tsx
  - app/compliance-audit/page.tsx
  - app/privacy/page.tsx
  - app/terms/page.tsx
  - lib/__tests__/seo-policy.test.ts
  - scripts/seo/check-seo-contract.mjs
  - docs/seo/canonical-and-indexing-policy.md
  - docs/seo/cwv-budgets.md
  - package.json
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-05
  - SEO-06
must_haves:
  truths:
    - "Every indexable page resolves metadata from one typed contract with unique title/description/canonical values."
    - "Robots and sitemap behavior match the same indexability policy, with no route drift."
    - "CWV budget thresholds are documented and enforced by an automated contract check command."
  artifacts:
    - path: "lib/seoPolicy.ts"
      provides: "Single source of truth for canonical ownership and indexability."
    - path: "app/sitemap.ts"
      provides: "Sitemap generation from SEO policy and dynamic slug sources."
    - path: "app/robots.ts"
      provides: "Robots directives aligned to shared SEO policy."
    - path: "lib/__tests__/seo-policy.test.ts"
      provides: "Uniqueness and canonical/indexing regression checks."
    - path: "scripts/seo/check-seo-contract.mjs"
      provides: "Automated gate for metadata contract and CWV budget constraints."
  key_links:
    - from: "app/sitemap.ts"
      to: "lib/seoPolicy.ts"
      via: "imports indexable route registry"
      pattern: "from \"@/lib/seoPolicy\""
    - from: "app/robots.ts"
      to: "lib/seoPolicy.ts"
      via: "reads disallow/noindex policy data"
    - from: "scripts/seo/check-seo-contract.mjs"
      to: "docs/seo/cwv-budgets.md"
      via: "uses documented thresholds as enforceable limits"
---

<objective>
Finalize metadata, canonical ownership, and indexing policy as one enforceable contract.

Purpose: Eliminate SEO drift across metadata, robots, and sitemap while establishing budgeted quality gates.
Output: Typed SEO policy module, refactored metadata integration points, and automated contract checks.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-seo-and-positioning-foundation/01-CONTEXT.md
@.planning/phases/01-seo-and-positioning-foundation/01-RESEARCH.md
@lib/seo.ts
@app/sitemap.ts
@app/robots.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create typed SEO policy and metadata ownership contract</name>
  <files>lib/seoPolicy.ts, lib/seo.ts</files>
  <behavior>
    - Contract exposes deterministic canonical and indexability policy per route/template type.
    - Metadata helper consumes policy (not ad-hoc route decisions) and preserves relative canonical format.
    - Contract can represent indexable and noindex utility routes without ambiguity.
  </behavior>
  <action>Introduce a new typed `lib/seoPolicy.ts` module that models route policy ownership (canonical path, page kind, indexability, and title/description pattern source). Refactor `lib/seo.ts` to consume this contract while keeping current public helper ergonomics. Preserve the user-locked decision: relative canonical paths resolved through `metadataBase`, and self-canonical ownership for indexable routes. Do not introduce cross-canonical consolidation rules in this phase.</action>
  <verify>
    <automated>npm run lint -- lib/seo.ts lib/seoPolicy.ts</automated>
  </verify>
  <done>`lib/seo.ts` and `lib/seoPolicy.ts` compile cleanly and expose one canonical/indexability contract used by downstream files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align route metadata, robots, and sitemap to shared policy</name>
  <files>app/robots.ts, app/sitemap.ts, app/page.tsx, app/about/page.tsx, app/blog/page.tsx, app/case-studies/page.tsx, app/guides/page.tsx, app/industries/page.tsx, app/contact/layout.tsx, app/calculator/layout.tsx, app/compliance-audit/page.tsx, app/privacy/page.tsx, app/terms/page.tsx</files>
  <behavior>
    - Indexability outcomes are consistent across page metadata, robots rules, and sitemap URLs.
    - Static marketing routes use deterministic metadata patterns from the shared contract.
    - Utility/noindex pages remain crawl-safe and intentionally excluded where required.
  </behavior>
  <action>Update route-level metadata callsites to consume deterministic policy data from the new contract. Refactor `app/robots.ts` and `app/sitemap.ts` to rely on the same policy registry so crawl/index behavior cannot drift. Ensure `/demo` and any utility routes remain noindex/disallowed as intended. Keep all indexable routes self-canonical and do not alter routing architecture.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build succeeds and route metadata, robots, and sitemap are synchronized via one policy contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add SEO contract checks and publish policy docs</name>
  <files>lib/__tests__/seo-policy.test.ts, scripts/seo/check-seo-contract.mjs, docs/seo/canonical-and-indexing-policy.md, docs/seo/cwv-budgets.md, package.json</files>
  <behavior>
    - Duplicate title/description/canonical conflicts are detected automatically.
    - Canonical ownership and indexing rules are documented for future phases.
    - CWV budget thresholds are defined and tied to an automated check command.
  </behavior>
  <action>Create regression tests for policy uniqueness and canonical/indexing expectations. Add a script (`scripts/seo/check-seo-contract.mjs`) that validates metadata contract integrity and enforces documented CWV budget thresholds from `docs/seo/cwv-budgets.md`. Add an npm script entry for this check. Keep docs concise and implementation-facing so later phases can reuse the same policy language.</action>
  <verify>
    <automated>npx vitest run lib/__tests__/seo-policy.test.ts && node scripts/seo/check-seo-contract.mjs</automated>
  </verify>
  <done>Automated contract checks pass, and policy + budget docs exist with enforceable guidance.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `npx vitest run lib/__tests__/seo-policy.test.ts`
- [ ] `node scripts/seo/check-seo-contract.mjs`
</verification>

<success_criteria>
- Metadata/canonical/indexability are controlled from one typed source.
- Robots and sitemap reflect the same route policy.
- Duplicate metadata/canonical conflicts are automatically caught.
- CWV budget definitions and enforcement command are in place.
</success_criteria>

<output>
After completion, create `.planning/phases/01-seo-and-positioning-foundation/01-01-SUMMARY.md`
</output>
