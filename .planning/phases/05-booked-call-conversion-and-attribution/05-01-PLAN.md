---
phase: 05-booked-call-conversion-and-attribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/lead/route.ts
  - app/contact/page.tsx
  - lib/booked-call-url.ts
  - lib/lead-contract.ts
  - components/BookedCallLink.tsx
  - components/Button.tsx
  - components/CTASection.tsx
  - app/features/[slug]/page.tsx
  - app/industries/[slug]/page.tsx
  - lib/__tests__/booked-call-url.test.ts
autonomous: true
requirements:
  - CRO-02
  - CRO-03
must_haves:
  truths:
    - "Contact/demo submission uses a real server endpoint and no longer returns simulated success."
    - "Priority feature and industry detail pages expose above-the-fold plus lower-page booked-call CTA paths."
    - "Booked-call links use one shared URL builder so attribution parameters are applied consistently."
  artifacts:
    - path: "app/api/lead/route.ts"
      provides: "Validated lead endpoint returning explicit success/failure JSON responses."
    - path: "app/contact/page.tsx"
      provides: "Client submit flow wired to /api/lead with retry-capable error handling."
    - path: "lib/booked-call-url.ts"
      provides: "Canonical booked-call URL builder that normalizes attribution params."
    - path: "components/BookedCallLink.tsx"
      provides: "Shared CTA component path for Cal.com links."
    - path: "lib/__tests__/booked-call-url.test.ts"
      provides: "Contract tests that prevent URL-param drift."
  key_links:
    - from: "app/contact/page.tsx"
      to: "/api/lead"
      via: "form submit fetch uses explicit result envelope"
      pattern: "fetch\\(\"/api/lead\""
    - from: "components/BookedCallLink.tsx"
      to: "lib/booked-call-url.ts"
      via: "link href is generated by shared URL builder"
      pattern: "buildBookedCallUrl"
    - from: "app/features/[slug]/page.tsx + app/industries/[slug]/page.tsx + components/CTASection.tsx"
      to: "components/BookedCallLink.tsx"
      via: "priority template CTAs route through shared booking-link component"
      pattern: "BookedCallLink"
---

<objective>
Replace simulated lead capture and normalize booked-call CTA paths on priority SEO templates.

Purpose: Fulfill CRO-03 and the priority-gated part of CRO-02 with production-ready conversion plumbing.
Output: A server-backed lead endpoint, shared booked-call URL contract, and priority CTA path standardization.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-booked-call-conversion-and-attribution/05-CONTEXT.md
@.planning/phases/05-booked-call-conversion-and-attribution/05-RESEARCH.md
@app/contact/page.tsx
@components/Button.tsx
@components/CTASection.tsx
@app/features/[slug]/page.tsx
@app/industries/[slug]/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build validated lead endpoint contract and response envelope</name>
  <files>app/api/lead/route.ts, lib/lead-contract.ts</files>
  <behavior>
    - Invalid payloads return 4xx with deterministic error codes/messages.
    - Valid payloads return success payload consumed by contact page state logic.
    - Endpoint supports optional downstream forwarding via environment configuration without hiding failures.
  </behavior>
  <action>Create `lib/lead-contract.ts` for request/response types and runtime validation helpers, then implement `app/api/lead/route.ts` POST handler using those contracts. Require `name` and `email`, keep existing optional fields, and return explicit `{ ok, message, errorCode }` JSON shape for all outcomes. If downstream forwarding is configured (for example webhook env var), surface downstream failures as explicit non-2xx responses instead of silent success.</action>
  <verify>
    <automated>npm run lint -- app/api/lead/route.ts lib/lead-contract.ts</automated>
  </verify>
  <done>Lead API endpoint is real, validated, and returns explicit success/failure semantics suitable for client UX handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace simulated contact submit flow with real endpoint integration</name>
  <files>app/contact/page.tsx</files>
  <behavior>
    - Form submit transitions remain `idle/submitting/success/error`.
    - Error state displays meaningful retry guidance with no silent success.
    - Success state only occurs after confirmed endpoint success.
  </behavior>
  <action>Remove the simulated timeout submission from `app/contact/page.tsx` and wire `handleSubmit` to `fetch("/api/lead", ...)` using the lead contract payload. Preserve current visual structure and field set. Add resilient error handling, parse API envelope safely, and maintain current reset path after success so UX shape is unchanged while behavior becomes production-reliable.</action>
  <verify>
    <automated>npm run lint -- app/contact/page.tsx && npm run build</automated>
  </verify>
  <done>Contact form behavior is real-endpoint-backed and only reports success/failure based on actual server result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement shared booked-call URL builder and wire priority CTAs</name>
  <files>lib/booked-call-url.ts, components/BookedCallLink.tsx, components/Button.tsx, components/CTASection.tsx, app/features/[slug]/page.tsx, app/industries/[slug]/page.tsx, lib/__tests__/booked-call-url.test.ts</files>
  <behavior>
    - Cal.com links for priority templates route through one shared builder/component path.
    - Builder preserves allowed attribution fields (`utm_*`, click IDs, referrer hints, landing URL, template/cluster tags).
    - Priority templates keep one above-the-fold and one lower-page CTA path.
  </behavior>
  <action>Create `lib/booked-call-url.ts` with canonical Cal.com base + deterministic param merge rules, plus `components/BookedCallLink.tsx` as shared CTA primitive for outbound booking links. Update `Button` integration where needed and replace direct booking links in `CTASection`, `app/features/[slug]/page.tsx`, and `app/industries/[slug]/page.tsx` with the shared component path. Add `lib/__tests__/booked-call-url.test.ts` to assert stable query merge behavior and drift prevention.</action>
  <verify>
    <automated>npx vitest run lib/__tests__/booked-call-url.test.ts && npm run lint -- lib/booked-call-url.ts components/BookedCallLink.tsx components/Button.tsx components/CTASection.tsx app/features/[slug]/page.tsx app/industries/[slug]/page.tsx</automated>
  </verify>
  <done>Priority template booked-call CTA paths are standardized and attribution-param attachment is contract-tested.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint -- app/api/lead/route.ts lib/lead-contract.ts app/contact/page.tsx lib/booked-call-url.ts components/BookedCallLink.tsx components/Button.tsx components/CTASection.tsx app/features/[slug]/page.tsx app/industries/[slug]/page.tsx`
- [ ] `npx vitest run lib/__tests__/booked-call-url.test.ts`
- [ ] `npm run build`
</verification>

<success_criteria>
- Simulated contact submission is fully replaced by server-backed behavior with explicit failure handling.
- Priority feature/industry pages have standardized above-fold and lower-page booked-call CTA paths.
- Shared booked-call URL contract prevents attribution-parameter drift across priority templates.
</success_criteria>

<output>
After completion, create `.planning/phases/05-booked-call-conversion-and-attribution/05-01-SUMMARY.md`
</output>

