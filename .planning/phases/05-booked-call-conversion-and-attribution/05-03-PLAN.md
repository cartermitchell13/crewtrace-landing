---
phase: 05-booked-call-conversion-and-attribution
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - scripts/analytics/export-booked-call-report.mjs
  - scripts/analytics/fixtures/booked-call-events.sample.ndjson
  - scripts/seo/check-booked-call-attribution.mjs
  - docs/seo/booked-call-reporting.md
  - package.json
  - components/BookedCallLink.tsx
  - app/compare/[slug]/page.tsx
  - app/guides/[slug]/page.tsx
  - app/case-studies/[slug]/page.tsx
  - app/blog/[slug]/page.tsx
autonomous: true
requirements:
  - CRO-01
  - CRO-02
  - CRO-05
must_haves:
  truths:
    - "Booked-call performance reporting can be exported and segmented by cluster, template type, and landing URL."
    - "Booked-call CTA link normalization is aligned across secondary SEO templates (compare, guides, case studies, blog)."
    - "Automation fails if required booked-call attribution wiring regresses on indexed template families."
  artifacts:
    - path: "scripts/analytics/export-booked-call-report.mjs"
      provides: "Deterministic aggregation/export of booked-call and lead metrics by cluster/template/landing URL."
    - path: "docs/seo/booked-call-reporting.md"
      provides: "Operational runbook for event export input and report generation outputs."
    - path: "scripts/seo/check-booked-call-attribution.mjs"
      provides: "Guardrail checks for shared CTA usage and instrumentation coverage on required templates."
    - path: "scripts/analytics/fixtures/booked-call-events.sample.ndjson"
      provides: "Stable fixture input for deterministic reporting script verification."
    - path: "app/compare/[slug]/page.tsx + app/guides/[slug]/page.tsx + app/case-studies/[slug]/page.tsx + app/blog/[slug]/page.tsx"
      provides: "Secondary template alignment to shared booked-call CTA contract."
  key_links:
    - from: "scripts/analytics/export-booked-call-report.mjs"
      to: "event rows emitted by /api/events"
      via: "ingests normalized event NDJSON and groups by cluster/template/landing dimensions"
      pattern: "cluster|template_type|landing_url"
    - from: "scripts/seo/check-booked-call-attribution.mjs"
      to: "template page files + shared BookedCallLink component"
      via: "asserts required templates route booked-call CTAs through shared contract path"
      pattern: "BookedCallLink"
    - from: "secondary template pages"
      to: "components/BookedCallLink.tsx"
      via: "all present booked-call CTAs use normalized shared booking-link path"
      pattern: "from \"@/components/BookedCallLink\""
---

<objective>
Deliver deterministic booked-call attribution reporting and enforce conversion wiring coverage.

Purpose: Fulfill CRO-05 and complete cross-template CTA attribution alignment for CRO-02/CRO-01 durability.
Output: Segmented reporting export script, guardrail checks, runbook docs, and secondary-template booked-call CTA normalization.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-booked-call-conversion-and-attribution/05-CONTEXT.md
@.planning/phases/05-booked-call-conversion-and-attribution/05-RESEARCH.md
@.planning/phases/05-booked-call-conversion-and-attribution/05-01-PLAN.md
@.planning/phases/05-booked-call-conversion-and-attribution/05-02-PLAN.md
@lib/seoPolicy.ts
@app/sitemap.ts
@components/BookedCallLink.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align secondary SEO templates to shared booked-call CTA contract</name>
  <files>components/BookedCallLink.tsx, app/compare/[slug]/page.tsx, app/guides/[slug]/page.tsx, app/case-studies/[slug]/page.tsx, app/blog/[slug]/page.tsx</files>
  <behavior>
    - Existing booked-call CTAs in secondary template families use shared BookedCallLink path.
    - CTA click events preserve template + cluster metadata for attribution segmentation.
    - No direct hardcoded Cal.com URLs remain in covered secondary templates.
  </behavior>
  <action>Update secondary SEO templates (`compare`, `guides`, `case-studies`, `blog`) so any booked-call CTA links flow through `BookedCallLink` and inherit standardized attribution behavior from Plans 01-02. Ensure emitted click metadata includes stable template/cluster values needed by reporting segmentation.</action>
  <verify>
    <automated>npm run lint -- components/BookedCallLink.tsx app/compare/[slug]/page.tsx app/guides/[slug]/page.tsx app/case-studies/[slug]/page.tsx app/blog/[slug]/page.tsx && npm run build</automated>
  </verify>
  <done>Secondary template booked-call CTAs are normalized and attribution-ready through one shared component path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build deterministic booked-call attribution reporting exporter</name>
  <files>scripts/analytics/export-booked-call-report.mjs, scripts/analytics/fixtures/booked-call-events.sample.ndjson</files>
  <behavior>
    - Input event rows can be aggregated by `cluster`, `template_type`, and `landing_url`.
    - Output includes booked-call click totals plus lead attempt/success/failure supporting metrics.
    - Script output is deterministic and reproducible with sample fixtures.
  </behavior>
  <action>Create `scripts/analytics/export-booked-call-report.mjs` that reads NDJSON event exports, normalizes required dimensions, and writes grouped JSON/CSV outputs. Include a deterministic fixture dataset in `scripts/analytics/fixtures/booked-call-events.sample.ndjson` and support CLI args for input/output paths so reports are reproducible across environments.</action>
  <verify>
    <automated>node scripts/analytics/export-booked-call-report.mjs --input scripts/analytics/fixtures/booked-call-events.sample.ndjson --output .planning/phases/05-booked-call-conversion-and-attribution/.tmp-report && rg --line-number \"cluster|template_type|landing_url|booked_call_cta_click\" .planning/phases/05-booked-call-conversion-and-attribution/.tmp-report/*</automated>
  </verify>
  <done>Reporting exporter consistently produces segmented booked-call attribution artifacts from normalized event inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add attribution guardrails, package scripts, and runbook documentation</name>
  <files>scripts/seo/check-booked-call-attribution.mjs, docs/seo/booked-call-reporting.md, package.json</files>
  <behavior>
    - Guardrail checks fail when required templates bypass shared booking-link path.
    - Package scripts expose deterministic commands for attribution check and report export.
    - Runbook defines required input shape, command usage, and expected segmented outputs.
  </behavior>
  <action>Add `scripts/seo/check-booked-call-attribution.mjs` to validate booked-call CTA contract coverage and required instrumentation markers across template families. Register commands in `package.json` (for example `seo:check-attribution` and `seo:report-booked-calls`). Document operator workflow in `docs/seo/booked-call-reporting.md`, including input event schema, command examples, and output interpretation rules.</action>
  <verify>
    <automated>node scripts/seo/check-booked-call-attribution.mjs && npm run seo:check-attribution && npm run seo:report-booked-calls -- --input scripts/analytics/fixtures/booked-call-events.sample.ndjson --output .planning/phases/05-booked-call-conversion-and-attribution/.tmp-report</automated>
  </verify>
  <done>Attribution reporting and coverage checks are repeatable, documented, and runnable from standard project scripts.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint -- components/BookedCallLink.tsx app/compare/[slug]/page.tsx app/guides/[slug]/page.tsx app/case-studies/[slug]/page.tsx app/blog/[slug]/page.tsx scripts/analytics/export-booked-call-report.mjs scripts/seo/check-booked-call-attribution.mjs`
- [ ] `node scripts/seo/check-booked-call-attribution.mjs`
- [ ] `node scripts/analytics/export-booked-call-report.mjs --input scripts/analytics/fixtures/booked-call-events.sample.ndjson --output .planning/phases/05-booked-call-conversion-and-attribution/.tmp-report`
- [ ] `npm run build`
</verification>

<success_criteria>
- Reporting exporter outputs booked-call conversion segments by cluster, template type, and landing URL.
- Secondary SEO template families with booked-call CTAs use the shared attribution contract path.
- Automated checks prevent regression of booked-call attribution coverage and reporting prerequisites.
</success_criteria>

<output>
After completion, create `.planning/phases/05-booked-call-conversion-and-attribution/05-03-SUMMARY.md`
</output>

