---
phase: 05-booked-call-conversion-and-attribution
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - lib/seo-events.ts
  - lib/first-touch-attribution.ts
  - lib/event-transport.ts
  - app/api/events/route.ts
  - components/SeoLandingTracker.tsx
  - components/BookedCallLink.tsx
  - app/layout.tsx
  - app/features/[slug]/page.tsx
  - app/industries/[slug]/page.tsx
  - app/contact/page.tsx
  - lib/__tests__/seo-events.test.ts
autonomous: true
requirements:
  - CRO-01
  - CRO-04
must_haves:
  truths:
    - "SEO landing sessions emit a standardized page-view attribution event on load."
    - "Booked-call CTA clicks and lead form lifecycle interactions emit explicit, stable event names."
    - "Booked-call conversion events carry first-touch attribution context needed to identify SEO-origin sessions."
  artifacts:
    - path: "lib/seo-events.ts"
      provides: "Typed event taxonomy and payload builders for all required conversion events."
    - path: "lib/first-touch-attribution.ts"
      provides: "First-touch capture/restore logic for utm/referrer/landing context."
    - path: "app/api/events/route.ts"
      provides: "Server ingestion endpoint for event payload normalization and forwarding."
    - path: "components/SeoLandingTracker.tsx"
      provides: "Reusable template-level event trigger for SEO landing page views."
    - path: "lib/__tests__/seo-events.test.ts"
      provides: "Contract assertions for event names, required fields, and attribution merge behavior."
  key_links:
    - from: "components/SeoLandingTracker.tsx"
      to: "lib/event-transport.ts + app/api/events/route.ts"
      via: "landing-view events are delivered through shared transport to ingestion endpoint"
      pattern: "sendSeoEvent"
    - from: "components/BookedCallLink.tsx + app/contact/page.tsx"
      to: "lib/seo-events.ts"
      via: "CTA click + lead attempt/success/failure events use typed taxonomy builders"
      pattern: "booked_call_cta_click|lead_form_submit_"
    - from: "lib/first-touch-attribution.ts"
      to: "all emitted conversion events"
      via: "first-touch context merged into payload before transport"
      pattern: "utm_|landing_url|referrer"
---

<objective>
Implement phase-wide attribution event taxonomy and transport for SEO conversion measurement.

Purpose: Fulfill CRO-01 and CRO-04 with explicit conversion telemetry tied to first-touch SEO context.
Output: Typed event contracts, first-touch attribution capture, ingestion endpoint, and priority-template instrumentation.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-booked-call-conversion-and-attribution/05-CONTEXT.md
@.planning/phases/05-booked-call-conversion-and-attribution/05-RESEARCH.md
@.planning/phases/05-booked-call-conversion-and-attribution/05-01-PLAN.md
@app/layout.tsx
@app/features/[slug]/page.tsx
@app/industries/[slug]/page.tsx
@app/contact/page.tsx
@components/BookedCallLink.tsx
@lib/seoPolicy.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed event taxonomy and first-touch attribution contract</name>
  <files>lib/seo-events.ts, lib/first-touch-attribution.ts, lib/__tests__/seo-events.test.ts</files>
  <behavior>
    - Event names exactly match required taxonomy (`seo_landing_view`, `booked_call_cta_click`, `lead_form_submit_attempt`, `lead_form_submit_success`, `lead_form_submit_failure`).
    - Required payload fields include template/cluster identifiers and first-touch attribution keys when available.
    - First-touch values persist and do not get overwritten by later navigation noise.
  </behavior>
  <action>Create `lib/seo-events.ts` for event enums/types/builders and `lib/first-touch-attribution.ts` for capturing/reading first-touch params (`utm_*`, click IDs, referrer, landing URL). Add unit tests in `lib/__tests__/seo-events.test.ts` to validate required keys, stable naming, and first-touch merge behavior with deterministic fixtures.</action>
  <verify>
    <automated>npx vitest run lib/__tests__/seo-events.test.ts && npm run lint -- lib/seo-events.ts lib/first-touch-attribution.ts</automated>
  </verify>
  <done>Event schema and attribution merge contracts are codified and tested before wiring templates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build shared event transport and server ingestion endpoint</name>
  <files>lib/event-transport.ts, app/api/events/route.ts</files>
  <behavior>
    - Client transport uses `navigator.sendBeacon` when available and falls back to `fetch` keepalive.
    - Ingestion endpoint validates payload envelope and returns explicit success/failure responses.
    - Transport and endpoint preserve deterministic payload structure for downstream reporting scripts.
  </behavior>
  <action>Implement `lib/event-transport.ts` with one `sendSeoEvent` entrypoint and fallback semantics for unload-safe delivery. Add `app/api/events/route.ts` route handler that validates incoming payload shape from `lib/seo-events.ts`, normalizes event timestamps, and returns explicit JSON outcome. Keep endpoint provider-agnostic so future storage/forwarding can swap without client changes.</action>
  <verify>
    <automated>npm run lint -- lib/event-transport.ts app/api/events/route.ts && npm run build</automated>
  </verify>
  <done>All conversion telemetry flows through one typed client transport and one validated server ingestion path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Instrument priority templates and lead flow with required events</name>
  <files>components/SeoLandingTracker.tsx, components/BookedCallLink.tsx, app/layout.tsx, app/features/[slug]/page.tsx, app/industries/[slug]/page.tsx, app/contact/page.tsx</files>
  <behavior>
    - SEO landing view fires on load for indexable priority templates with template + cluster metadata.
    - Booked-call CTA click events fire from shared booking-link component path.
    - Lead form attempt/success/failure events fire around `/api/lead` submit lifecycle.
  </behavior>
  <action>Add `components/SeoLandingTracker.tsx` and mount instrumentation from template pages via explicit props (`cluster`, `templateType`, `landingUrl`). Extend `BookedCallLink` to emit `booked_call_cta_click` with first-touch context before navigation. Update contact submit flow to emit lead-form lifecycle events around API calls. Initialize first-touch bootstrap in `app/layout.tsx` client boundary without changing page UX layout.</action>
  <verify>
    <automated>npm run lint -- components/SeoLandingTracker.tsx components/BookedCallLink.tsx app/layout.tsx app/features/[slug]/page.tsx app/industries/[slug]/page.tsx app/contact/page.tsx && npm run build</automated>
  </verify>
  <done>Priority SEO templates and lead flow emit complete attribution-ready events through the shared taxonomy and transport stack.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint -- lib/seo-events.ts lib/first-touch-attribution.ts lib/event-transport.ts app/api/events/route.ts components/SeoLandingTracker.tsx components/BookedCallLink.tsx app/layout.tsx app/features/[slug]/page.tsx app/industries/[slug]/page.tsx app/contact/page.tsx`
- [ ] `npx vitest run lib/__tests__/seo-events.test.ts`
- [ ] `npm run build`
</verification>

<success_criteria>
- Required SEO landing and conversion interaction events are emitted with stable names and attribution payloads.
- First-touch attribution context persists and is merged into booked-call and lead-form events.
- Event transport + ingestion pathway is standardized and ready for reporting aggregation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-booked-call-conversion-and-attribution/05-02-SUMMARY.md`
</output>

