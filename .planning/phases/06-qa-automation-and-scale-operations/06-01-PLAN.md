---
phase: 06-qa-automation-and-scale-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/seo-quality.yml
  - package.json
  - scripts/seo/check-seo-contract.mjs
  - scripts/seo/check-schema-presence.mjs
  - scripts/seo/check-internal-links.mjs
  - scripts/seo/lib/route-inventory.mjs
  - lib/__tests__/seo-policy.test.ts
  - lib/__tests__/schema.test.ts
  - docs/seo/qa-quality-gates.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "CI fails on duplicate metadata/canonical policy conflicts before merge."
    - "CI fails when indexable pages contain broken internal links."
    - "CI fails when required structured data coverage regresses on protected templates."
  artifacts:
    - path: ".github/workflows/seo-quality.yml"
      provides: "Release-blocking CI workflow that runs SEO contract checks plus build/test validation."
    - path: "scripts/seo/check-seo-contract.mjs"
      provides: "Deterministic metadata/canonical conflict checks aligned to SEO policy."
    - path: "scripts/seo/check-internal-links.mjs"
      provides: "Broken internal-link checker for indexable routes."
    - path: "scripts/seo/lib/route-inventory.mjs"
      provides: "Reusable inventory of valid route targets from static policy and dynamic slug contracts."
    - path: "scripts/seo/check-schema-presence.mjs"
      provides: "Required schema coverage checks per template type."
  key_links:
    - from: ".github/workflows/seo-quality.yml"
      to: "scripts/seo/check-seo-contract.mjs + scripts/seo/check-internal-links.mjs + scripts/seo/check-schema-presence.mjs"
      via: "workflow executes deterministic guardrail scripts as blocking steps"
      pattern: "node scripts/seo/check-"
    - from: "scripts/seo/check-internal-links.mjs"
      to: "scripts/seo/lib/route-inventory.mjs + app/sitemap.ts + lib/seoPolicy.ts"
      via: "validator resolves href targets against indexable route inventory"
      pattern: "isIndexablePath|solutionSlugs|industrySlugs"
    - from: "lib/__tests__/seo-policy.test.ts + lib/__tests__/schema.test.ts"
      to: "CI workflow"
      via: "test assertions protect policy/schema behavior beyond one-off scripts"
      pattern: "npx vitest run"
---

<objective>
Implement release-blocking CI quality gates for metadata, internal links, and schema coverage.

Purpose: Fulfill OPS-01, OPS-02, and OPS-03 by making SEO quality regressions impossible to merge silently.
Output: A CI workflow and deterministic script suite that fail fast on metadata/canonical, link, and schema violations.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-qa-automation-and-scale-operations/06-RESEARCH.md
@scripts/seo/check-seo-contract.mjs
@scripts/seo/check-schema-presence.mjs
@scripts/seo/check-cluster-content.mjs
@app/sitemap.ts
@lib/seo.ts
@lib/seoPolicy.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden metadata/canonical and schema guardrail contracts</name>
  <files>scripts/seo/check-seo-contract.mjs, scripts/seo/check-schema-presence.mjs, lib/__tests__/seo-policy.test.ts, lib/__tests__/schema.test.ts</files>
  <behavior>
    - Duplicate title, description, and canonical ownership conflicts fail with explicit error output.
    - Schema checks cover all required template families and required token presence.
    - Guardrail tests remain deterministic and runnable in CI without external services.
  </behavior>
  <action>Extend `check-seo-contract.mjs` so canonical conflict detection is explicit and aligned with `createPageMetadata` path ownership rules. Tighten `check-schema-presence.mjs` template coverage and required token diagnostics. Add or update focused Vitest assertions (`seo-policy`, `schema`) to protect helper behavior and avoid script drift.</action>
  <verify>
    <automated>node scripts/seo/check-seo-contract.mjs && node scripts/seo/check-schema-presence.mjs && npx vitest run lib/__tests__/seo-policy.test.ts lib/__tests__/schema.test.ts</automated>
  </verify>
  <done>Metadata/canonical and schema regressions are blocked by both script guardrails and targeted tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add indexable internal-link integrity checker</name>
  <files>scripts/seo/check-internal-links.mjs, scripts/seo/lib/route-inventory.mjs</files>
  <behavior>
    - Indexable templates are scanned for internal href targets and normalized route paths.
    - Links to missing/non-indexable targets fail with actionable diagnostics.
    - Dynamic slug-backed routes are validated from shared data contracts, not hand-maintained lists.
  </behavior>
  <action>Create `scripts/seo/lib/route-inventory.mjs` that builds a route inventory from static SEO policy plus dynamic slug/content exports. Implement `scripts/seo/check-internal-links.mjs` to parse internal links from protected template files and verify each target resolves in the inventory (excluding explicitly allowed utility/legal exceptions). Follow existing guardrail output style: grouped failures with clear source file and href.</action>
  <verify>
    <automated>node scripts/seo/check-internal-links.mjs</automated>
  </verify>
  <done>Broken internal links on indexable pages are detected deterministically and fail CI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire quality gate scripts into blocking CI workflow</name>
  <files>.github/workflows/seo-quality.yml, package.json, docs/seo/qa-quality-gates.md</files>
  <behavior>
    - Pull requests and main pushes run quality gates before merge/deploy.
    - CI step ordering keeps fast-fail script checks before heavier build work.
    - Operators have one runbook documenting local commands and expected failures.
  </behavior>
  <action>Add `.github/workflows/seo-quality.yml` with jobs for install, script guardrails, targeted tests, lint, and build. Register a consolidated script command in `package.json` (for example `seo:check:all`) that runs metadata, schema, link, cluster, messaging, and attribution checks. Document workflow behavior and local reproduction in `docs/seo/qa-quality-gates.md`.</action>
  <verify>
    <automated>npm run seo:check:all && npx vitest run lib/__tests__/seo-policy.test.ts lib/__tests__/schema.test.ts && npm run lint && npm run build</automated>
  </verify>
  <done>CI quality gates are release-blocking and reproducible locally for SEO contract, links, and schema regressions.</done>
</task>

</tasks>

<verification>
- [ ] `npm run seo:check:all`
- [ ] `npx vitest run lib/__tests__/seo-policy.test.ts lib/__tests__/schema.test.ts`
- [ ] `npm run lint`
- [ ] `npm run build`
</verification>

<success_criteria>
- Metadata/canonical conflicts, broken internal links, and schema regressions fail CI by default.
- Route inventory for link validation is derived from shared SEO/slug contracts, not fragile manual lists.
- Operators can reproduce the same quality-gate checks locally with one command.
</success_criteria>

<output>
After completion, create `.planning/phases/06-qa-automation-and-scale-operations/06-01-SUMMARY.md`
</output>

