---
phase: 06-qa-automation-and-scale-operations
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - .github/workflows/weekly-seo-report.yml
  - scripts/analytics/export-weekly-seo-ops-report.mjs
  - scripts/analytics/lib/weekly-report-cluster-map.mjs
  - scripts/analytics/fixtures/search-console-indexed.sample.csv
  - scripts/analytics/fixtures/search-console-traffic.sample.csv
  - scripts/analytics/fixtures/booked-call-report.sample.json
  - docs/seo/weekly-seo-operations-reporting.md
autonomous: true
requirements:
  - OPS-05
must_haves:
  truths:
    - "A weekly report artifact is generated with indexed pages, traffic trend, and booked calls by cluster."
    - "Report output schema is deterministic and stable for week-over-week comparison."
    - "Report generation is automated on a recurring schedule without manual spreadsheet assembly."
  artifacts:
    - path: "scripts/analytics/export-weekly-seo-ops-report.mjs"
      provides: "Exporter that aggregates indexation, traffic trend, and booked-call metrics into one report."
    - path: "scripts/analytics/lib/weekly-report-cluster-map.mjs"
      provides: "Shared URL-to-cluster mapping logic for consistent segmentation."
    - path: ".github/workflows/weekly-seo-report.yml"
      provides: "Scheduled workflow that runs exporter and publishes report artifacts."
    - path: "docs/seo/weekly-seo-operations-reporting.md"
      provides: "Runbook for report inputs, schedule behavior, and output interpretation."
    - path: "scripts/analytics/fixtures/*.csv + *.json"
      provides: "Deterministic sample inputs for exporter verification."
  key_links:
    - from: "scripts/analytics/export-weekly-seo-ops-report.mjs"
      to: "scripts/analytics/lib/weekly-report-cluster-map.mjs"
      via: "normalizes URLs into stable cluster segments for all metrics"
      pattern: "cluster|features|industries|compare|guides|case-studies|blog"
    - from: "weekly report exporter"
      to: "scripts/analytics/export-booked-call-report.mjs outputs"
      via: "consumes booked-call segmented metrics as one weekly input stream"
      pattern: "booked_call_cta_click|lead_form_submit_success"
    - from: ".github/workflows/weekly-seo-report.yml"
      to: "export-weekly-seo-ops-report.mjs"
      via: "cron-triggered workflow executes exporter and uploads artifacts"
      pattern: "schedule|upload-artifact"
---

<objective>
Automate weekly SEO operations reporting with indexation, traffic trend, and booked-call cluster metrics.

Purpose: Fulfill OPS-05 by replacing ad hoc reporting with a deterministic scheduled export pipeline.
Output: Weekly report exporter, schedule automation, fixture coverage, and operations runbook.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-qa-automation-and-scale-operations/06-RESEARCH.md
@.planning/phases/06-qa-automation-and-scale-operations/06-01-PLAN.md
@scripts/analytics/export-booked-call-report.mjs
@docs/seo/booked-call-reporting.md
@lib/seoPolicy.ts
@app/sitemap.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define weekly report input/output contract and deterministic fixtures</name>
  <files>scripts/analytics/fixtures/search-console-indexed.sample.csv, scripts/analytics/fixtures/search-console-traffic.sample.csv, scripts/analytics/fixtures/booked-call-report.sample.json, docs/seo/weekly-seo-operations-reporting.md</files>
  <behavior>
    - Input contracts explicitly define indexed-page rows, traffic trend rows, and booked-call segmented rows.
    - Fixtures represent realistic multi-cluster weekly data with stable expected totals.
    - Runbook documents required columns, optional fields, and failure behavior.
  </behavior>
  <action>Create fixture inputs for indexed pages, traffic trend, and booked-call segment metrics with stable deterministic values. Document the weekly report schema, input file expectations, and output fields in `weekly-seo-operations-reporting.md` before implementing exporter logic so contract drift is minimized.</action>
  <verify>
    <automated>rg --line-number \"cluster|indexed|traffic|booked\" scripts/analytics/fixtures/search-console-indexed.sample.csv scripts/analytics/fixtures/search-console-traffic.sample.csv scripts/analytics/fixtures/booked-call-report.sample.json docs/seo/weekly-seo-operations-reporting.md</automated>
  </verify>
  <done>Weekly report contracts are codified with deterministic sample data and operator documentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exporter and cluster normalization for weekly operations reporting</name>
  <files>scripts/analytics/export-weekly-seo-ops-report.mjs, scripts/analytics/lib/weekly-report-cluster-map.mjs</files>
  <behavior>
    - Exporter produces one weekly artifact with indexed count, traffic trend, and booked-call metrics by cluster.
    - Cluster mapping from URLs is stable and shared in one helper module.
    - Output includes both machine-readable JSON and analyst-friendly CSV rows.
  </behavior>
  <action>Implement `weekly-report-cluster-map.mjs` for deterministic URL-to-cluster resolution, then build `export-weekly-seo-ops-report.mjs` to ingest indexation and traffic CSV inputs plus booked-call JSON input. Aggregate into weekly cluster summaries and write JSON/CSV outputs with explicit timestamps and schema version. Keep parser logic resilient to column ordering while failing fast on missing required fields.</action>
  <verify>
    <automated>node scripts/analytics/export-weekly-seo-ops-report.mjs --indexed scripts/analytics/fixtures/search-console-indexed.sample.csv --traffic scripts/analytics/fixtures/search-console-traffic.sample.csv --booked scripts/analytics/fixtures/booked-call-report.sample.json --output .planning/phases/06-qa-automation-and-scale-operations/.tmp-weekly-report && rg --line-number \"indexed_pages|traffic_trend|booked_calls|cluster\" .planning/phases/06-qa-automation-and-scale-operations/.tmp-weekly-report/*</automated>
  </verify>
  <done>Exporter generates deterministic weekly cluster reporting artifacts covering all OPS-05 dimensions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Schedule automated weekly report generation in CI</name>
  <files>.github/workflows/weekly-seo-report.yml, docs/seo/weekly-seo-operations-reporting.md</files>
  <behavior>
    - Workflow runs on a weekly cron and manual dispatch for on-demand reruns.
    - Workflow executes exporter and uploads generated JSON/CSV artifacts.
    - Runbook describes schedule timing, required inputs, and artifact retrieval.
  </behavior>
  <action>Add `.github/workflows/weekly-seo-report.yml` with weekly cron trigger and `workflow_dispatch`, then run exporter using configured input paths/secrets conventions and upload outputs as workflow artifacts. Update runbook with schedule cadence, local fallback command, and remediation guidance when data inputs are missing.</action>
  <verify>
    <automated>node scripts/analytics/export-weekly-seo-ops-report.mjs --indexed scripts/analytics/fixtures/search-console-indexed.sample.csv --traffic scripts/analytics/fixtures/search-console-traffic.sample.csv --booked scripts/analytics/fixtures/booked-call-report.sample.json --output .planning/phases/06-qa-automation-and-scale-operations/.tmp-weekly-report && rg --line-number \"schedule|workflow_dispatch|upload-artifact\" .github/workflows/weekly-seo-report.yml</automated>
  </verify>
  <done>Weekly SEO operations reporting runs automatically and publishes deterministic artifacts without manual assembly.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/analytics/export-weekly-seo-ops-report.mjs --indexed scripts/analytics/fixtures/search-console-indexed.sample.csv --traffic scripts/analytics/fixtures/search-console-traffic.sample.csv --booked scripts/analytics/fixtures/booked-call-report.sample.json --output .planning/phases/06-qa-automation-and-scale-operations/.tmp-weekly-report`
- [ ] `rg --line-number "indexed_pages|traffic_trend|booked_calls|cluster" .planning/phases/06-qa-automation-and-scale-operations/.tmp-weekly-report/*`
- [ ] `rg --line-number "schedule|workflow_dispatch|upload-artifact" .github/workflows/weekly-seo-report.yml`
</verification>

<success_criteria>
- Weekly report includes indexed pages, traffic trend, and booked-call metrics segmented by cluster.
- Output contract is deterministic and reusable for week-over-week comparison and audits.
- Scheduled workflow produces report artifacts automatically with documented operator runbook.
</success_criteria>

<output>
After completion, create `.planning/phases/06-qa-automation-and-scale-operations/06-03-SUMMARY.md`
</output>

