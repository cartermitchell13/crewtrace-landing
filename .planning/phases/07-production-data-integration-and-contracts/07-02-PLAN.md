---
phase: 07-production-data-integration-and-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/seo-events.ts
  - lib/first-touch-attribution.ts
  - lib/__tests__/seo-events.test.ts
  - components/CalEmbedInteractionTracker.tsx
  - app/contact/page.tsx
  - scripts/analytics/validate-ga4-readiness.mjs
  - scripts/analytics/fixtures/ga4/ga4-events.weekly.sample.csv
  - docs/seo/ga4-event-contract.md
autonomous: true
requirements:
  - DATA-05
  - DATA-03
must_haves:
  truths:
    - "Required GA4 conversion events and attribution dimensions are explicitly validated."
    - "`booked_call_embed_interaction` is captured without requiring a full contact-flow replacement."
    - "Booked-call and downstream conversion events carry deterministic attribution keys for joins."
  artifacts:
    - path: "lib/seo-events.ts"
      provides: "Extended event taxonomy/parsing with GA4-required events and deterministic attribution key field."
    - path: "components/CalEmbedInteractionTracker.tsx"
      provides: "Client tracker for Cal embed interaction events mapped to SEO conversion taxonomy."
    - path: "scripts/analytics/validate-ga4-readiness.mjs"
      provides: "GA4 contract validator for required event names and dimensions."
    - path: "docs/seo/ga4-event-contract.md"
      provides: "GA4 event/dimension contract and validation runbook."
  key_links:
    - from: "components/CalEmbedInteractionTracker.tsx"
      to: "lib/seo-events.ts + lib/event-transport.ts"
      via: "embed interaction listener builds and sends booked-call embed interaction events through shared transport"
      pattern: "booked_call_embed_interaction|sendSeoEvent|postMessage"
    - from: "scripts/analytics/validate-ga4-readiness.mjs"
      to: "scripts/analytics/fixtures/ga4/ga4-events.weekly.sample.csv"
      via: "validator checks required event + dimension contract from GA4 export rows"
      pattern: "seo_landing_view|booked_call_cta_click|booked_call_embed_interaction|cluster|template_type|landing_url"
---

<objective>
Lock the GA4 conversion contract and deterministic attribution keys for SEO conversion reporting.

Purpose: Fulfill DATA-05 and DATA-03 by ensuring required GA4 events/dimensions are enforced and conversion events remain joinable by stable keys.
Output: Updated event taxonomy + embed interaction tracking, GA4 readiness validator, and contract documentation.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-production-data-integration-and-contracts/07-CONTEXT.md
@.planning/phases/07-production-data-integration-and-contracts/07-RESEARCH.md
@lib/seo-events.ts
@lib/first-touch-attribution.ts
@lib/event-transport.ts
@components/BookedCallLink.tsx
@app/contact/page.tsx
@docs/seo/booked-call-reporting.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend conversion event contract with deterministic attribution keys</name>
  <files>lib/seo-events.ts, lib/first-touch-attribution.ts, lib/__tests__/seo-events.test.ts</files>
  <behavior>
    - Event taxonomy includes `booked_call_embed_interaction` in the required tracked set.
    - Conversion payloads include a deterministic attribution key usable across ingestion and report joins.
    - Contract tests lock expected event names, key derivation, and first-touch landing preservation.
  </behavior>
  <action>Update `seo-events.ts` to support `booked_call_embed_interaction` and a stable attribution/conversion key derived from first-touch context plus canonical landing dimensions. Keep parser/build helpers backward-safe while requiring key presence for new payloads. Update `seo-events.test.ts` assertions and add any required first-touch helpers in `first-touch-attribution.ts` so key derivation is deterministic for identical inputs.</action>
  <verify>
    <automated>npx vitest run lib/__tests__/seo-events.test.ts</automated>
  </verify>
  <done>Event contract supports required GA4 taxonomy and deterministic key propagation with locked tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture Cal embed interaction events without replacing contact flow</name>
  <files>components/CalEmbedInteractionTracker.tsx, app/contact/page.tsx</files>
  <behavior>
    - Embed interaction tracker listens to Cal embed interaction signals and emits SEO event payloads.
    - Contact page instrumentation adds interaction tracking without replacing lead-form behavior.
    - Tracker emits required attribution dimensions and deterministic conversion key fields.
  </behavior>
  <action>Create `CalEmbedInteractionTracker.tsx` as a client component that listens for Cal embed interaction signals (for example `postMessage` events) and sends `booked_call_embed_interaction` through shared event transport helpers. Integrate tracker into `app/contact/page.tsx` so interaction tracking is active while preserving existing submit and CTA event flows.</action>
  <verify>
    <automated>rg --line-number "CalEmbedInteractionTracker|booked_call_embed_interaction|sendSeoEvent|postMessage" components/CalEmbedInteractionTracker.tsx app/contact/page.tsx</automated>
  </verify>
  <done>Embed interaction telemetry is emitted reliably and remains in-scope with no contact-flow replacement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add GA4 readiness validator and fixture contract</name>
  <files>scripts/analytics/validate-ga4-readiness.mjs, scripts/analytics/fixtures/ga4/ga4-events.weekly.sample.csv, docs/seo/ga4-event-contract.md</files>
  <behavior>
    - Validator fails when required event names or required attribution dimensions are missing.
    - Fixture data captures pass/fail-ready examples for deterministic contract testing.
    - Runbook documents expected GA4 export columns and release-gate criteria.
  </behavior>
  <action>Implement `validate-ga4-readiness.mjs` to parse GA4 export rows and enforce required event names (`seo_landing_view`, `booked_call_cta_click`, `booked_call_embed_interaction`) plus required dimensions (`cluster`, `template_type`, `landing_url`). Add deterministic fixture rows and document contract setup, local validation command, and release-gate expectation in `ga4-event-contract.md`.</action>
  <verify>
    <automated>node scripts/analytics/validate-ga4-readiness.mjs --input scripts/analytics/fixtures/ga4/ga4-events.weekly.sample.csv</automated>
  </verify>
  <done>GA4 contract readiness is validated deterministically and documented for release gating.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run lib/__tests__/seo-events.test.ts`
- [ ] `node scripts/analytics/validate-ga4-readiness.mjs --input scripts/analytics/fixtures/ga4/ga4-events.weekly.sample.csv`
- [ ] `rg --line-number "booked_call_embed_interaction|cluster|template_type|landing_url" lib/seo-events.ts scripts/analytics/validate-ga4-readiness.mjs docs/seo/ga4-event-contract.md`
</verification>

<success_criteria>
- Required GA4 event and dimension contract is explicit, testable, and validated by script.
- Embed interaction event tracking is added in-scope without replacing contact experience.
- Deterministic attribution keys are available for downstream report joins.
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-data-integration-and-contracts/07-02-SUMMARY.md`
</output>

