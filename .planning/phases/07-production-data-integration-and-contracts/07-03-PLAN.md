---
phase: 07-production-data-integration-and-contracts
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - scripts/analytics/export-booked-call-report.mjs
  - scripts/analytics/export-weekly-seo-ops-report.mjs
  - scripts/analytics/build-weekly-seo-production-report.mjs
  - scripts/analytics/lib/weekly-report-cluster-map.mjs
  - .github/workflows/weekly-seo-report.yml
  - .github/workflows/seo-quality.yml
  - package.json
  - docs/seo/weekly-seo-operations-reporting.md
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - OPS-10
must_haves:
  truths:
    - "Weekly reporting runs against production-ingested inputs and no longer depends on fixture-only release paths."
    - "Booked-call outcomes remain joinable to first-touch landing dimensions through deterministic conversion keys."
    - "Release CI blocks malformed ingestion or reporting artifacts before merge/deploy."
  artifacts:
    - path: "scripts/analytics/build-weekly-seo-production-report.mjs"
      provides: "Orchestrator that composes ingestion, booked-call export, and weekly report generation for one UTC week."
    - path: "scripts/analytics/export-booked-call-report.mjs"
      provides: "Booked-call aggregation output with deterministic conversion-key and first-touch landing join fields."
    - path: "scripts/analytics/export-weekly-seo-ops-report.mjs"
      provides: "Weekly report exporter consuming validated production-ingested artifacts."
    - path: ".github/workflows/weekly-seo-report.yml"
      provides: "Scheduled workflow that runs production report pipeline and uploads outputs."
    - path: ".github/workflows/seo-quality.yml"
      provides: "Release-blocking workflow that includes ingestion/reporting contract validation checks."
  key_links:
    - from: "scripts/analytics/build-weekly-seo-production-report.mjs"
      to: "scripts/analytics/fetch-production-source-data.mjs + scripts/analytics/export-booked-call-report.mjs + scripts/analytics/export-weekly-seo-ops-report.mjs"
      via: "pipeline orchestrator runs validated ingestion then export joins for one weekly window"
      pattern: "fetch-production-source-data|export-booked-call-report|export-weekly-seo-ops-report"
    - from: ".github/workflows/seo-quality.yml"
      to: "scripts/seo/check-production-ingestion-contract.mjs + scripts/analytics/validate-ga4-readiness.mjs"
      via: "release gate executes data-contract validators before build"
      pattern: "check-production-ingestion-contract|validate-ga4-readiness"
    - from: "scripts/analytics/export-weekly-seo-ops-report.mjs"
      to: "scripts/analytics/lib/weekly-report-cluster-map.mjs"
      via: "all joined rows normalize landing URLs and map deterministic clusters/templates"
      pattern: "normalizeUrlPath|mapUrlToCluster|cluster"
---

<objective>
Integrate production ingestion, attribution joins, and CI gates into the weekly SEO reporting pipeline.

Purpose: Fulfill DATA-01, DATA-02, DATA-03, DATA-04, and OPS-10 by wiring Phase 7 contracts into scheduled reporting and release-blocking validation.
Output: End-to-end production weekly report pipeline orchestration, joined export updates, and CI/workflow gate wiring.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-production-data-integration-and-contracts/07-CONTEXT.md
@.planning/phases/07-production-data-integration-and-contracts/07-RESEARCH.md
@.planning/phases/07-production-data-integration-and-contracts/07-01-PLAN.md
@.planning/phases/07-production-data-integration-and-contracts/07-02-PLAN.md
@scripts/analytics/export-booked-call-report.mjs
@scripts/analytics/export-weekly-seo-ops-report.mjs
@scripts/analytics/lib/weekly-report-cluster-map.mjs
@.github/workflows/weekly-seo-report.yml
@.github/workflows/seo-quality.yml
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic join keys and first-touch fields to booked-call export</name>
  <files>scripts/analytics/export-booked-call-report.mjs</files>
  <behavior>
    - Export rows include deterministic conversion key and first-touch landing attribution fields required for joins.
    - Aggregation remains deterministic for cluster/template/landing dimensions.
    - Join fields are explicit in both JSON and CSV outputs.
  </behavior>
  <action>Extend `export-booked-call-report.mjs` to preserve deterministic conversion keys and first-touch landing fields from ingested events, and emit those fields in output rows and summary metadata. Keep required dimensions and existing metrics backward-compatible while adding explicit join diagnostics for missing keys.</action>
  <verify>
    <automated>npm run seo:report-booked-calls -- --input scripts/analytics/fixtures/booked-call-events.sample.ndjson --output .planning/phases/07-production-data-integration-and-contracts/.tmp-booked-report && rg --line-number "conversion_key|first_touch|landing_url|booked_call_cta_click_count" .planning/phases/07-production-data-integration-and-contracts/.tmp-booked-report/*</automated>
  </verify>
  <done>Booked-call report emits deterministic joinable fields required by downstream weekly reporting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build production weekly pipeline orchestrator and integrate normalized joins</name>
  <files>scripts/analytics/build-weekly-seo-production-report.mjs, scripts/analytics/export-weekly-seo-ops-report.mjs, scripts/analytics/lib/weekly-report-cluster-map.mjs, docs/seo/weekly-seo-operations-reporting.md</files>
  <behavior>
    - One orchestrator command runs ingestion, booked-call export, and weekly report generation for target week.
    - Weekly exporter consumes production-ingested artifacts and keeps deterministic schema/order.
    - URL, cluster, and template normalization is consistent across ingest, join, and weekly output.
  </behavior>
  <action>Create `build-weekly-seo-production-report.mjs` to orchestrate `fetch-production-source-data`, `export-booked-call-report`, and `export-weekly-seo-ops-report` with explicit week boundaries and freeze semantics. Update weekly exporter and normalization helpers as needed so canonical URL/cluster/template mappings are applied consistently across all source rows. Update reporting runbook with production command flow and remediation steps for missing sources or join-key failures.</action>
  <verify>
    <automated>node scripts/analytics/build-weekly-seo-production-report.mjs --week 2026-02-23 --indexed scripts/analytics/fixtures/production/search-console-indexed.weekly.sample.csv --traffic scripts/analytics/fixtures/production/search-console-traffic.weekly.sample.csv --booked-events scripts/analytics/fixtures/production/booked-call-events.weekly.sample.ndjson --ga4 scripts/analytics/fixtures/ga4/ga4-events.weekly.sample.csv --output .planning/phases/07-production-data-integration-and-contracts/.tmp-weekly-production && rg --line-number "schema_version|week_start|week_end|cluster|template|landing_url" .planning/phases/07-production-data-integration-and-contracts/.tmp-weekly-production/*</automated>
  </verify>
  <done>End-to-end weekly production reporting can run deterministically from validated source inputs with consistent normalization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire ingestion and GA4 contract checks into blocking CI workflows</name>
  <files>.github/workflows/weekly-seo-report.yml, .github/workflows/seo-quality.yml, package.json</files>
  <behavior>
    - Release workflow blocks on ingestion contract and GA4 readiness failures.
    - Weekly scheduled workflow runs production pipeline command (not fixture-only direct exporter call).
    - Local script aliases support reproducible contract and report checks.
  </behavior>
  <action>Add script aliases in `package.json` for production report pipeline and GA4/ingestion contract checks. Update `seo-quality.yml` to run those checks in blocking mode before build. Update `weekly-seo-report.yml` to execute `build-weekly-seo-production-report.mjs` with production-configured inputs/secrets and upload resulting JSON/CSV artifacts.</action>
  <verify>
    <automated>rg --line-number "check-production-ingestion-contract|validate-ga4-readiness|build-weekly-seo-production-report|seo:check:all|schedule|upload-artifact" package.json .github/workflows/seo-quality.yml .github/workflows/weekly-seo-report.yml</automated>
  </verify>
  <done>CI and scheduled workflows enforce data-contract quality gates and run the production reporting pipeline.</done>
</task>

</tasks>

<verification>
- [ ] `npm run seo:report-booked-calls -- --input scripts/analytics/fixtures/booked-call-events.sample.ndjson --output .planning/phases/07-production-data-integration-and-contracts/.tmp-booked-report`
- [ ] `node scripts/analytics/build-weekly-seo-production-report.mjs --week 2026-02-23 --indexed scripts/analytics/fixtures/production/search-console-indexed.weekly.sample.csv --traffic scripts/analytics/fixtures/production/search-console-traffic.weekly.sample.csv --booked-events scripts/analytics/fixtures/production/booked-call-events.weekly.sample.ndjson --ga4 scripts/analytics/fixtures/ga4/ga4-events.weekly.sample.csv --output .planning/phases/07-production-data-integration-and-contracts/.tmp-weekly-production`
- [ ] `npm run seo:check:all`
</verification>

<success_criteria>
- Weekly reporting release path uses production-ingested inputs with deterministic windowing and joins.
- Attribution joins preserve first-touch landing dimensions and deterministic conversion keys.
- CI blocks malformed ingestion/reporting contracts before merge and scheduled workflow automation remains reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-data-integration-and-contracts/07-03-SUMMARY.md`
</output>

