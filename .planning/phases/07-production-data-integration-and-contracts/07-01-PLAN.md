---
phase: 07-production-data-integration-and-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/analytics/lib/weekly-report-window.mjs
  - scripts/analytics/lib/production-source-contracts.mjs
  - scripts/analytics/fetch-production-source-data.mjs
  - scripts/analytics/fixtures/production/search-console-indexed.weekly.sample.csv
  - scripts/analytics/fixtures/production/search-console-traffic.weekly.sample.csv
  - scripts/analytics/fixtures/production/booked-call-events.weekly.sample.ndjson
  - scripts/seo/check-production-ingestion-contract.mjs
  - docs/seo/production-data-contracts.md
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-04
must_haves:
  truths:
    - "Weekly ingestion reads production-shaped source inputs instead of fixture-only report paths."
    - "Malformed or incomplete source rows fail fast before reporting/export steps run."
    - "Source rows are filtered to previous Monday-Sunday UTC using occurrence timestamps."
  artifacts:
    - path: "scripts/analytics/lib/weekly-report-window.mjs"
      provides: "Deterministic UTC week-boundary and freeze-window helper for ingestion/reporting scripts."
    - path: "scripts/analytics/lib/production-source-contracts.mjs"
      provides: "Source-specific required field contract definitions and validators."
    - path: "scripts/analytics/fetch-production-source-data.mjs"
      provides: "Ingestion entrypoint that fetches, validates, normalizes, and writes weekly source artifacts."
    - path: "scripts/seo/check-production-ingestion-contract.mjs"
      provides: "Blocking contract check for ingestion source completeness and schema validity."
    - path: "docs/seo/production-data-contracts.md"
      provides: "Operator runbook for source contracts, weekly window rules, and failure diagnostics."
  key_links:
    - from: "scripts/analytics/fetch-production-source-data.mjs"
      to: "scripts/analytics/lib/production-source-contracts.mjs + scripts/analytics/lib/weekly-report-window.mjs"
      via: "ingestion flow validates source rows against contract and filters by UTC weekly window"
      pattern: "validate|required|week_start|week_end|occurred_at"
    - from: "scripts/seo/check-production-ingestion-contract.mjs"
      to: "scripts/analytics/fetch-production-source-data.mjs"
      via: "guardrail script executes ingestion contract checks before downstream reporting"
      pattern: "fetch-production-source-data|contract|missing"
---

<objective>
Define and enforce production ingestion contracts for weekly SEO reporting inputs.

Purpose: Fulfill DATA-01, DATA-02, and DATA-04 by replacing fixture-only assumptions with validated production-shaped ingestion artifacts.
Output: Weekly window helper, source contract validator, ingestion script, and production ingestion runbook.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-production-data-integration-and-contracts/07-CONTEXT.md
@.planning/phases/07-production-data-integration-and-contracts/07-RESEARCH.md
@scripts/analytics/export-weekly-seo-ops-report.mjs
@scripts/analytics/export-booked-call-report.mjs
@scripts/analytics/lib/weekly-report-cluster-map.mjs
@docs/seo/weekly-seo-operations-reporting.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Codify UTC weekly window and required source contracts</name>
  <files>scripts/analytics/lib/weekly-report-window.mjs, scripts/analytics/lib/production-source-contracts.mjs, docs/seo/production-data-contracts.md</files>
  <behavior>
    - Previous Monday-Sunday UTC window resolution is deterministic and reusable.
    - Required source fields are centralized for indexed, traffic, booked-call, and GA4 source payloads.
    - Contract docs explicitly define fail-fast behavior for missing datasets and malformed rows.
  </behavior>
  <action>Create `weekly-report-window.mjs` to resolve prior-week boundaries and freeze metadata using source occurrence timestamps. Create `production-source-contracts.mjs` as single source of required columns/fields and per-source validators. Document contract semantics, weekly freeze behavior, and failure diagnostics in `production-data-contracts.md`.</action>
  <verify>
    <automated>rg --line-number "previous|Monday|Sunday|UTC|required|missing|freeze" scripts/analytics/lib/weekly-report-window.mjs scripts/analytics/lib/production-source-contracts.mjs docs/seo/production-data-contracts.md</automated>
  </verify>
  <done>Windowing and source schema contracts are explicit, reusable, and documented for operators.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement production-source ingestion with fail-fast validation</name>
  <files>scripts/analytics/fetch-production-source-data.mjs, scripts/analytics/fixtures/production/search-console-indexed.weekly.sample.csv, scripts/analytics/fixtures/production/search-console-traffic.weekly.sample.csv, scripts/analytics/fixtures/production/booked-call-events.weekly.sample.ndjson</files>
  <behavior>
    - Ingestion loads source rows, enforces required fields, and fails immediately on malformed rows.
    - Output artifacts are normalized for downstream booked-call and weekly exporters.
    - Inclusion logic is based on source occurrence timestamps in UTC.
  </behavior>
  <action>Implement `fetch-production-source-data.mjs` to read production or fixture-equivalent inputs, validate each source against `production-source-contracts.mjs`, filter to prior-week UTC via `weekly-report-window.mjs`, and emit normalized intermediate artifacts to an output directory. Add deterministic production-shaped fixture samples under `scripts/analytics/fixtures/production` for local and CI validation.</action>
  <verify>
    <automated>node scripts/analytics/fetch-production-source-data.mjs --week 2026-02-23 --indexed scripts/analytics/fixtures/production/search-console-indexed.weekly.sample.csv --traffic scripts/analytics/fixtures/production/search-console-traffic.weekly.sample.csv --booked-events scripts/analytics/fixtures/production/booked-call-events.weekly.sample.ndjson --output .planning/phases/07-production-data-integration-and-contracts/.tmp-production-ingest</automated>
  </verify>
  <done>Ingestion produces deterministic, validated weekly source artifacts and rejects malformed inputs with actionable errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add blocking ingestion contract guardrail script</name>
  <files>scripts/seo/check-production-ingestion-contract.mjs, docs/seo/production-data-contracts.md</files>
  <behavior>
    - Guardrail script validates source completeness and required schema before reporting steps.
    - Diagnostics indicate missing source datasets or columns with clear source labels.
    - Script can run against fixture paths for deterministic CI and local smoke checks.
  </behavior>
  <action>Create `check-production-ingestion-contract.mjs` that calls shared ingestion-contract validators and fails non-zero when required datasets or columns are missing for the target week. Update runbook with local command examples and expected pass/fail output.</action>
  <verify>
    <automated>node scripts/seo/check-production-ingestion-contract.mjs --indexed scripts/analytics/fixtures/production/search-console-indexed.weekly.sample.csv --traffic scripts/analytics/fixtures/production/search-console-traffic.weekly.sample.csv --booked-events scripts/analytics/fixtures/production/booked-call-events.weekly.sample.ndjson</automated>
  </verify>
  <done>Production ingestion contract validation is deterministic and ready for CI gating integration.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/analytics/fetch-production-source-data.mjs --week 2026-02-23 --indexed scripts/analytics/fixtures/production/search-console-indexed.weekly.sample.csv --traffic scripts/analytics/fixtures/production/search-console-traffic.weekly.sample.csv --booked-events scripts/analytics/fixtures/production/booked-call-events.weekly.sample.ndjson --output .planning/phases/07-production-data-integration-and-contracts/.tmp-production-ingest`
- [ ] `node scripts/seo/check-production-ingestion-contract.mjs --indexed scripts/analytics/fixtures/production/search-console-indexed.weekly.sample.csv --traffic scripts/analytics/fixtures/production/search-console-traffic.weekly.sample.csv --booked-events scripts/analytics/fixtures/production/booked-call-events.weekly.sample.ndjson`
</verification>

<success_criteria>
- Weekly ingestion contract enforces production-shaped source completeness and schema validity.
- Previous Monday-Sunday UTC filtering is deterministic and codified in shared helper logic.
- Normalized ingestion artifacts are ready for downstream attribution/reporting plans.
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-data-integration-and-contracts/07-01-SUMMARY.md`
</output>

