---
phase: 02-priority-cluster-build-features-industries
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app/features/page.tsx
  - app/features/[slug]/page.tsx
  - app/industries/page.tsx
  - app/industries/[slug]/page.tsx
  - lib/seoPolicy.ts
  - scripts/seo/check-seo-contract.mjs
  - scripts/seo/check-schema-presence.mjs
autonomous: true
requirements:
  - CLUS-01
  - CLUS-02
  - CLUS-03
must_haves:
  truths:
    - "Both feature and industry templates render from shared typed data records."
    - "A feature hub page exists and links to every feature detail page."
    - "Industry and feature hubs provide crawlable entry points into both clusters."
  artifacts:
    - path: "app/features/page.tsx"
      provides: "Feature hub route generated from typed feature records."
    - path: "app/features/[slug]/page.tsx"
      provides: "Feature detail template fully sourced from contract-backed feature records."
    - path: "app/industries/page.tsx"
      provides: "Industry hub page sourced from shared industry records."
    - path: "app/industries/[slug]/page.tsx"
      provides: "Industry detail template sourced from shared industry records."
    - path: "lib/seoPolicy.ts"
      provides: "Static policy coverage for the feature hub route."
  key_links:
    - from: "app/features/page.tsx"
      to: "app/features/[slug]/page.tsx"
      via: "hub links generated from feature slug records"
      pattern: "href={`/features/${"
    - from: "app/industries/page.tsx"
      to: "app/industries/[slug]/page.tsx"
      via: "hub links generated from industry slug records"
      pattern: "href={`/industries/${"
    - from: "app/industries/[slug]/page.tsx"
      to: "app/features/[slug]/page.tsx"
      via: "related solution links sourced from typed relationships"
      pattern: "href={`/features/${"
---

<objective>
Refactor feature and industry templates to use schema-backed data contracts and complete hub crawl paths.

Purpose: Remove inline/manual cluster page logic so expansion remains deterministic and crawl-safe.
Output: Contract-backed feature and industry hubs/detail routes with SEO-policy and schema-check alignment.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-priority-cluster-build-features-industries/02-RESEARCH.md
@.planning/phases/02-priority-cluster-build-features-industries/02-01-PLAN.md
@lib/solutions.ts
@lib/industries.ts
@app/features/[slug]/page.tsx
@app/industries/page.tsx
@app/industries/[slug]/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build feature hub route from typed feature records</name>
  <files>app/features/page.tsx, lib/seoPolicy.ts, scripts/seo/check-seo-contract.mjs</files>
  <behavior>
    - `/features` is an indexable hub route with policy-backed metadata ownership.
    - Hub cards are generated from feature records and include crawlable links to all feature detail pages.
    - SEO policy checks include the new static hub route.
  </behavior>
  <action>Create `app/features/page.tsx` as a full hub template generated from typed feature records (not hardcoded JSX rows). Add `/features` to `lib/seoPolicy.ts` static route policies and extend `scripts/seo/check-seo-contract.mjs` to enforce policy/metadata expectations for the new page. Keep the page aligned with existing messaging guardrails in `lib/messaging.ts` and existing visual language.</action>
  <verify>
    <automated>node scripts/seo/check-seo-contract.mjs && npm run lint -- app/features/page.tsx lib/seoPolicy.ts</automated>
  </verify>
  <done>The feature hub exists, passes SEO contract checks, and links to all feature child pages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor feature and industry detail templates to shared contracts</name>
  <files>app/features/[slug]/page.tsx, app/industries/[slug]/page.tsx</files>
  <behavior>
    - Detail templates consume only exported contract modules for record retrieval and static params.
    - No route-local inline data maps remain in detail templates.
    - Existing schema and messaging helpers continue to be used.
  </behavior>
  <action>Update both detail routes to read data from `lib/solutions.ts` and `lib/industries.ts` exports only. Remove inline industry mapping logic from `app/industries/[slug]/page.tsx` and preserve 404 handling, metadata generation, schema injection, and related-link behavior. Keep routing and visual sections stable while replacing data source ownership.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build succeeds and detail templates are fully contract-backed with no inline data definitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor industry hub to contract-backed rendering and schema guardrails</name>
  <files>app/industries/page.tsx, scripts/seo/check-schema-presence.mjs</files>
  <behavior>
    - Industry hub cards are generated from shared industry records.
    - Hub links cover all industries exported by contract data.
    - Schema coverage checks remain valid after template refactors.
  </behavior>
  <action>Refactor `app/industries/page.tsx` to consume `lib/industries.ts` record exports rather than maintaining a local inline list. Confirm feature cross-links are generated from declared relationships. Update `scripts/seo/check-schema-presence.mjs` if needed so schema requirements still match the refactored template topology and continue guarding detail template schema usage.</action>
  <verify>
    <automated>node scripts/seo/check-schema-presence.mjs && npm run build</automated>
  </verify>
  <done>Industry hub is fully contract-backed, crawl links are complete, and schema checks pass.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `node scripts/seo/check-seo-contract.mjs`
- [ ] `node scripts/seo/check-schema-presence.mjs`
</verification>

<success_criteria>
- `/features` and `/industries` hubs are both contract-backed crawl entry points.
- Feature and industry detail pages no longer rely on hand-edited route-local data maps.
- SEO/schema guardrails remain green after refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/02-priority-cluster-build-features-industries/02-02-SUMMARY.md`
</output>
