---
phase: 02-priority-cluster-build-features-industries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/solutions.ts
  - lib/industries.ts
  - app/sitemap.ts
  - lib/__tests__/cluster-content.test.ts
  - scripts/seo/check-cluster-content.mjs
  - docs/seo/cluster-intent-ownership.md
  - package.json
autonomous: true
requirements:
  - CLUS-01
  - CLUS-02
  - CLUS-06
must_haves:
  truths:
    - "Feature and industry records are defined in typed contract modules instead of inline route objects."
    - "Each feature and industry record has one primary intent field suitable for URL ownership checks."
    - "Automated checks fail when cluster records are missing required fields or duplicate intent ownership."
  artifacts:
    - path: "lib/solutions.ts"
      provides: "Typed feature records, slug exports, and lookup helpers with explicit intent fields."
    - path: "lib/industries.ts"
      provides: "Typed industry records, slug exports, and lookup helpers consumed by templates."
    - path: "scripts/seo/check-cluster-content.mjs"
      provides: "Automated guardrail for record shape and intent ownership uniqueness."
    - path: "lib/__tests__/cluster-content.test.ts"
      provides: "Regression tests for typed content contracts and ownership invariants."
    - path: "docs/seo/cluster-intent-ownership.md"
      provides: "Human-readable ownership policy used by implementation and review."
  key_links:
    - from: "app/sitemap.ts"
      to: "lib/industries.ts"
      via: "imports industry slugs from shared contract module"
      pattern: "from \"@/lib/industries\""
    - from: "scripts/seo/check-cluster-content.mjs"
      to: "lib/solutions.ts"
      via: "validates feature record shape and intent uniqueness"
    - from: "scripts/seo/check-cluster-content.mjs"
      to: "lib/industries.ts"
      via: "validates industry record shape and intent uniqueness"
---

<objective>
Build typed feature and industry data contracts with deterministic validators.

Purpose: Create one scalable source of truth for both priority clusters before template refactors and coverage expansion.
Output: Typed record modules, contract tests, and script-level guardrails for intent ownership.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-priority-cluster-build-features-industries/02-RESEARCH.md
@.planning/phases/01-seo-and-positioning-foundation/01-01-SUMMARY.md
@.planning/phases/01-seo-and-positioning-foundation/01-02-SUMMARY.md
@lib/solutions.ts
@app/industries/[slug]/page.tsx
@app/sitemap.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed contracts for feature and industry records</name>
  <files>lib/solutions.ts, lib/industries.ts</files>
  <behavior>
    - Feature records expose required fields for slug, keyword, intent, and relationship links.
    - Industry records are moved from inline route data into a shared typed module.
    - Both modules export slug arrays and slug-indexed lookups for route and sitemap usage.
  </behavior>
  <action>Refactor `lib/solutions.ts` into an explicit feature-record contract with a required `primaryIntent` ownership field and stable helper exports. Create `lib/industries.ts` with typed industry records equivalent to current page capabilities (hero copy, stats, benefits, related solutions, and metadata inputs) plus required `primaryIntent`. Preserve existing slugs unless an intentional migration is documented in this plan's summary.</action>
  <verify>
    <automated>npm run lint -- lib/solutions.ts lib/industries.ts</automated>
  </verify>
  <done>Both cluster domains compile as typed data contracts and expose deterministic slug/map helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add contract tests for shape and intent ownership</name>
  <files>lib/__tests__/cluster-content.test.ts</files>
  <behavior>
    - Tests assert required fields are present for every feature and industry record.
    - Tests fail on duplicate `primaryIntent` values within each cluster.
    - Tests confirm slug-list exports align with map exports.
  </behavior>
  <action>Create `lib/__tests__/cluster-content.test.ts` that validates typed contract invariants for both modules: required properties, non-empty record collections, slug/map consistency, and per-cluster intent uniqueness. Keep tests deterministic and fast so they can run as part of routine CI checks.</action>
  <verify>
    <automated>npx vitest run lib/__tests__/cluster-content.test.ts</automated>
  </verify>
  <done>Contract tests pass and fail correctly when fields/intents are intentionally broken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add cluster guardrail script, docs, and command wiring</name>
  <files>scripts/seo/check-cluster-content.mjs, docs/seo/cluster-intent-ownership.md, app/sitemap.ts, package.json</files>
  <behavior>
    - Script enforces contract shape and intent ownership outside test-only contexts.
    - Sitemap consumes shared slug exports instead of hardcoded industry arrays.
    - Ownership policy is documented with practical review rules.
  </behavior>
  <action>Implement `scripts/seo/check-cluster-content.mjs` to validate both cluster modules for required keys, duplicate slug risk, and duplicate `primaryIntent` ownership. Update `app/sitemap.ts` to source all industry and feature slugs from shared data modules. Document ownership rules and naming standards in `docs/seo/cluster-intent-ownership.md`, then add a package script (for example `seo:check-clusters`) so the guardrail is runnable in CI/local workflows.</action>
  <verify>
    <automated>node scripts/seo/check-cluster-content.mjs && npm run build</automated>
  </verify>
  <done>Cluster guardrail script passes, sitemap uses shared slug exports, and ownership policy documentation exists.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint`
- [ ] `npx vitest run lib/__tests__/cluster-content.test.ts`
- [ ] `node scripts/seo/check-cluster-content.mjs`
- [ ] `npm run build`
</verification>

<success_criteria>
- Feature and industry page sources are typed contracts in `lib/`.
- Cluster intent ownership is encoded and machine-checked.
- Sitemap slug generation depends on shared contract exports, not duplicate lists.
</success_criteria>

<output>
After completion, create `.planning/phases/02-priority-cluster-build-features-industries/02-01-SUMMARY.md`
</output>
