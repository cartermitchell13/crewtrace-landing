---
phase: 02-priority-cluster-build-features-industries
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - lib/solutions.ts
  - lib/industries.ts
  - app/features/page.tsx
  - app/industries/page.tsx
  - app/features/[slug]/page.tsx
  - app/industries/[slug]/page.tsx
  - lib/__tests__/cluster-coverage.test.ts
  - scripts/seo/check-cluster-content.mjs
  - docs/seo/priority-cluster-coverage.md
autonomous: true
requirements:
  - CLUS-03
  - CLUS-05
  - CLUS-06
must_haves:
  truths:
    - "Priority trades include construction, HVAC, waterproofing, general contractors, and landscaping with live pages in the cluster graph."
    - "Feature and industry hubs expose crawlable paths to all priority-trade child pages."
    - "Intent ownership and trade coverage checks fail automatically when requirements regress."
  artifacts:
    - path: "lib/industries.ts"
      provides: "Priority-trade industry records and relationship metadata."
    - path: "lib/solutions.ts"
      provides: "Feature records aligned to priority-trade coverage and intent ownership."
    - path: "docs/seo/priority-cluster-coverage.md"
      provides: "Coverage matrix for required priority trades and associated cluster pages."
    - path: "lib/__tests__/cluster-coverage.test.ts"
      provides: "Automated coverage assertions for required trades and link-path expectations."
    - path: "scripts/seo/check-cluster-content.mjs"
      provides: "Script-level enforcement for priority coverage and non-overlapping intents."
  key_links:
    - from: "app/features/page.tsx"
      to: "app/features/[slug]/page.tsx"
      via: "feature hub cards include all feature slugs for crawl access"
    - from: "app/industries/page.tsx"
      to: "app/industries/[slug]/page.tsx"
      via: "industry hub cards include all industry slugs including required priority trades"
    - from: "app/industries/[slug]/page.tsx"
      to: "app/features/[slug]/page.tsx"
      via: "related feature links ensure trade pages connect to capability pages"
---

<objective>
Expand and verify priority-trade coverage while preserving intent ownership and crawl completeness.

Purpose: Ensure the two core clusters cover required trade demand with enforceable quality and ownership rules.
Output: Priority coverage expansion, cross-cluster crawl-path reinforcement, and automated regression checks.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-priority-cluster-build-features-industries/02-RESEARCH.md
@.planning/phases/02-priority-cluster-build-features-industries/02-01-PLAN.md
@.planning/phases/02-priority-cluster-build-features-industries/02-02-PLAN.md
@lib/solutions.ts
@lib/industries.ts
@app/features/page.tsx
@app/industries/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand typed records to full priority-trade coverage</name>
  <files>lib/solutions.ts, lib/industries.ts, docs/seo/priority-cluster-coverage.md</files>
  <behavior>
    - Priority trades include construction, hvac, waterproofing, general-contractors, and landscaping in industry records.
    - Feature records map cleanly to priority-trade coverage with one primary intent per URL.
    - Coverage expectations are documented in an explicit matrix.
  </behavior>
  <action>Extend feature and industry record sets to guarantee required priority-trade presence (including waterproofing where missing). Keep one `primaryIntent` per page and avoid introducing intent overlap during expansion. Add `docs/seo/priority-cluster-coverage.md` with a matrix of required trades, their industry pages, and linked feature pages so coverage decisions remain auditable.</action>
  <verify>
    <automated>npm run lint -- lib/solutions.ts lib/industries.ts</automated>
  </verify>
  <done>Priority trade coverage data and intent ownership fields are complete and documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ensure hub and detail templates expose complete priority crawl paths</name>
  <files>app/features/page.tsx, app/industries/page.tsx, app/features/[slug]/page.tsx, app/industries/[slug]/page.tsx</files>
  <behavior>
    - Every priority-trade industry page is reachable from the industry hub.
    - Every feature page is reachable from the feature hub.
    - Industry detail pages include links into related feature pages for cross-cluster discovery.
  </behavior>
  <action>Update hub/detail templates so rendered link sets match the expanded priority-trade contracts. Keep link generation data-driven from typed record relationships, not hand-curated JSX. Ensure there are no missing links for newly added priority pages and preserve existing messaging/schema integrations while adjusting link surfaces.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build passes and cluster hubs/details provide deterministic crawl paths for all priority records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add coverage and ownership regression checks</name>
  <files>lib/__tests__/cluster-coverage.test.ts, scripts/seo/check-cluster-content.mjs</files>
  <behavior>
    - Tests verify required priority trades are present and linked.
    - Script checks fail on missing required-trade coverage and duplicate intent ownership.
    - Validation commands are fast enough for routine execution.
  </behavior>
  <action>Create `lib/__tests__/cluster-coverage.test.ts` to assert required priority trades and cross-cluster relationship expectations. Extend `scripts/seo/check-cluster-content.mjs` to enforce required-trade presence and ownership constraints in addition to record shape checks from Plan 02-01. Keep failure messages explicit so regressions can be fixed quickly.</action>
  <verify>
    <automated>npx vitest run lib/__tests__/cluster-content.test.ts lib/__tests__/cluster-coverage.test.ts && node scripts/seo/check-cluster-content.mjs</automated>
  </verify>
  <done>Coverage and ownership checks pass and fail correctly when required priority data is removed or duplicated.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `npx vitest run lib/__tests__/cluster-content.test.ts lib/__tests__/cluster-coverage.test.ts`
- [ ] `node scripts/seo/check-cluster-content.mjs`
</verification>

<success_criteria>
- Required priority trades are represented and linked across feature and industry clusters.
- Crawl paths from hubs to child pages are complete and deterministic.
- Priority coverage and intent ownership regressions are blocked by automated checks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-priority-cluster-build-features-industries/02-03-SUMMARY.md`
</output>
