---
phase: 04-competitor-and-authority-content
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - app/compare/page.tsx
  - app/compare/[slug]/page.tsx
  - app/sitemap.ts
  - lib/seoPolicy.ts
  - scripts/seo/check-seo-contract.mjs
autonomous: true
requirements:
  - COMP-02
must_haves:
  truths:
    - "Dedicated competitor pages are published for Connecteam and Workyard with neutral, factual framing and soft consultative CTAs."
    - "Competitor pages contextually route users into relevant feature, industry, and proof-asset pages."
    - "Competitor pages are crawlable via sitemap and index policy without being added to homepage or primary navigation."
  artifacts:
    - path: "app/compare/page.tsx"
      provides: "Low-visibility competitor-intent hub used as an internal and sitemap surface."
    - path: "app/compare/[slug]/page.tsx"
      provides: "Competitor detail template rendered from typed contracts with metadata/schema support."
    - path: "app/sitemap.ts"
      provides: "Competitor route entries generated from typed competitor slug exports."
    - path: "lib/seoPolicy.ts"
      provides: "Canonical/index policy coverage for competitor hub and dynamic route prefix."
    - path: "scripts/seo/check-seo-contract.mjs"
      provides: "Contract check coverage for competitor static metadata policy registration."
  key_links:
    - from: "app/compare/[slug]/page.tsx"
      to: "lib/competitors.ts"
      via: "route params resolve competitor detail content from typed contracts"
      pattern: "from \"@/lib/competitors\""
    - from: "app/compare/[slug]/page.tsx"
      to: "/features/* + /industries/* + /guides/* or /case-studies/*"
      via: "contextual CTA/resource links from competitor records"
      pattern: "href={`/features/"
    - from: "app/sitemap.ts"
      to: "lib/competitors.ts"
      via: "competitor slug exports feed sitemap URL generation"
      pattern: "/compare/${slug}"
---

<objective>
Publish competitor-intent pages and connect them into existing crawl pathways without primary-nav exposure.

Purpose: Fulfill COMP-02 through dedicated comparison pages that can rank safely while preserving current brand navigation strategy.
Output: Competitor route templates, metadata/index integration, and sitemap discoverability.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-competitor-and-authority-content/04-CONTEXT.md
@.planning/phases/04-competitor-and-authority-content/04-RESEARCH.md
@.planning/phases/04-competitor-and-authority-content/04-01-PLAN.md
@lib/competitors.ts
@lib/seo.ts
@lib/schema.ts
@lib/seoPolicy.ts
@app/features/[slug]/page.tsx
@app/industries/[slug]/page.tsx
@app/sitemap.ts
@components/Navbar.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build competitor hub and dynamic competitor detail routes</name>
  <files>app/compare/page.tsx, app/compare/[slug]/page.tsx, lib/competitors.ts</files>
  <behavior>
    - `/compare` lists launch competitor intents and routes to individual detail pages.
    - `/compare/[slug]` is statically generated for all competitor slugs.
    - Detail pages render neutral comparison framing, freshness metadata, and soft consultative CTA copy.
  </behavior>
  <action>Create `app/compare/page.tsx` and `app/compare/[slug]/page.tsx` using current route patterns (`generateStaticParams`, `generateMetadata`, data lookup + `notFound()`). Render copy and sections from `lib/competitors.ts` only. Use `createPageMetadata`, plus `articleSchema` and `breadcrumbSchema`, and keep tone factual while preserving conversion intent through consultative CTA language.</action>
  <verify>
    <automated>npm run lint -- app/compare/page.tsx app/compare/[slug]/page.tsx lib/competitors.ts && npm run build</automated>
  </verify>
  <done>Competitor hub/detail pages compile, generate metadata/schema correctly, and render fully from typed contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire competitor pages into cluster and proof-asset pathways</name>
  <files>app/compare/[slug]/page.tsx, lib/competitors.ts</files>
  <behavior>
    - Each competitor page links to relevant feature and industry pages.
    - Each competitor page links to at least one proof asset (guide/case study).
    - Internal links are contextual and deterministic (no hardcoded ad hoc route strings in JSX).
  </behavior>
  <action>Render contextual link modules on competitor detail pages using slug arrays from `lib/competitors.ts`, resolving target labels via existing `lib/solutions.ts`, `lib/industries.ts`, `lib/guides.ts`, and `lib/caseStudies.ts` exports. Keep link counts bounded and avoid adding competitor links to global nav components.</action>
  <verify>
    <automated>npx vitest run lib/__tests__/competitor-content.test.ts && node scripts/seo/check-competitor-content.mjs</automated>
  </verify>
  <done>Competitor pages expose reliable internal paths into priority clusters and proof assets for both ranking and conversion support.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate competitor pages into sitemap and SEO contract policy</name>
  <files>app/sitemap.ts, lib/seoPolicy.ts, scripts/seo/check-seo-contract.mjs</files>
  <behavior>
    - Competitor routes appear in generated sitemap output.
    - SEO policy explicitly covers competitor hub plus dynamic route prefix.
    - Contract checks include competitor static route metadata registration.
  </behavior>
  <action>Update `app/sitemap.ts` to include `/compare/${slug}` entries from `competitorSlugs`. Extend `lib/seoPolicy.ts` with static policy for `/compare` and dynamic prefix coverage for `/compare/`. Update `scripts/seo/check-seo-contract.mjs` to include `app/compare/page.tsx` in the static metadata check set. Add a verification assertion that `components/Navbar.tsx` and homepage do not introduce top-level `/compare` links.</action>
  <verify>
    <automated>npm run seo:check && node -e "const fs=require('fs'); const nav=fs.readFileSync('components/Navbar.tsx','utf8'); const home=fs.readFileSync('app/page.tsx','utf8'); if(nav.includes('/compare')||home.includes('/compare')){process.exit(1);}"</automated>
  </verify>
  <done>Competitor pages are indexable and discoverable through crawl infrastructure while remaining absent from primary nav/home exposure.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint -- app/compare/page.tsx app/compare/[slug]/page.tsx app/sitemap.ts lib/seoPolicy.ts scripts/seo/check-seo-contract.mjs`
- [ ] `npm run build`
- [ ] `npm run seo:check`
- [ ] `node scripts/seo/check-competitor-content.mjs`
</verification>

<success_criteria>
- Dedicated competitor pages are live as typed-contract routes.
- Pages include safe comparison framing, freshness metadata, and soft CTA pathways.
- Crawl/index policy includes competitor routes without adding nav/home prominence.
</success_criteria>

<output>
After completion, create `.planning/phases/04-competitor-and-authority-content/04-02-SUMMARY.md`
</output>
