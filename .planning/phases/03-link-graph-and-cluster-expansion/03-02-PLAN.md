---
phase: 03-link-graph-and-cluster-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - lib/solutions.ts
  - lib/industries.ts
  - app/features/page.tsx
  - app/industries/page.tsx
  - app/sitemap.ts
  - docs/seo/priority-cluster-coverage.md
  - lib/__tests__/cluster-content.test.ts
  - lib/__tests__/cluster-coverage.test.ts
  - scripts/seo/check-cluster-content.mjs
autonomous: true
requirements:
  - CLUS-04
must_haves:
  truths:
    - "Any newly added feature/industry expansion page has at least one parent and one sibling internal link."
    - "Expansion pages retain unique primary intent ownership and reciprocal cross-cluster relevance."
    - "Expanded pages appear in crawl surfaces (hubs + sitemap) without creating orphan risk."
  artifacts:
    - path: "lib/solutions.ts"
      provides: "Expanded feature records with reciprocal industry mappings and non-overlapping primary intents."
    - path: "lib/industries.ts"
      provides: "Expanded industry records with reciprocal feature mappings and non-overlapping primary intents."
    - path: "docs/seo/priority-cluster-coverage.md"
      provides: "Updated expansion coverage matrix with explicit relationship ownership."
    - path: "app/sitemap.ts"
      provides: "Sitemap includes expanded slugs via shared contract exports."
    - path: "scripts/seo/check-cluster-content.mjs"
      provides: "Eligibility and non-orphan validation for expansion records."
  key_links:
    - from: "app/features/page.tsx"
      to: "app/features/[slug]/page.tsx"
      via: "hub links generated from expanded feature contract summaries"
      pattern: "href={`/features/${feature.slug}`}"
    - from: "app/industries/page.tsx"
      to: "app/industries/[slug]/page.tsx"
      via: "hub links generated from expanded industry contract summaries"
      pattern: "href={`/industries/${industry.slug}`}"
    - from: "app/sitemap.ts"
      to: "lib/solutions.ts + lib/industries.ts"
      via: "dynamic sitemap entries sourced from shared slug exports"
---

<objective>
Add safe cluster expansion pages with enforced link and intent validation.

Purpose: Expand high-intent feature/industry coverage without violating CLUS-04 parent/sibling link guarantees or intent ownership boundaries.
Output: Expanded contract records, crawl-surface continuity, and stricter expansion validation gates.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-link-graph-and-cluster-expansion/03-CONTEXT.md
@.planning/phases/03-link-graph-and-cluster-expansion/03-RESEARCH.md
@.planning/phases/03-link-graph-and-cluster-expansion/03-01-PLAN.md
@docs/seo/priority-cluster-coverage.md
@lib/solutions.ts
@lib/industries.ts
@app/features/page.tsx
@app/industries/page.tsx
@app/sitemap.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand typed records under strict eligibility rules</name>
  <files>lib/solutions.ts, lib/industries.ts, docs/seo/priority-cluster-coverage.md</files>
  <behavior>
    - New expansion records include unique `primaryIntent` ownership within cluster.
    - Cross-cluster links are reciprocal for every new relationship.
    - New records can produce at least one sibling relation under Phase 3 graph rules.
  </behavior>
  <action>Add expansion feature/industry records only where eligibility conditions from `03-CONTEXT.md` are satisfied (unique intent, reciprocal links, sibling participation). Update `docs/seo/priority-cluster-coverage.md` with expansion rows and relationship ownership notes so additions are auditable and reviewable.</action>
  <verify>
    <automated>npm run lint -- lib/solutions.ts lib/industries.ts docs/seo/priority-cluster-coverage.md</automated>
  </verify>
  <done>Expansion records are contract-backed, reciprocal, and documented with explicit intent ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ensure hubs and sitemap expose expanded crawl paths</name>
  <files>app/features/page.tsx, app/industries/page.tsx, app/sitemap.ts</files>
  <behavior>
    - Expanded records are discoverable from corresponding hub pages.
    - Expanded records are represented in sitemap through shared slug exports.
    - No route-local arrays are introduced during expansion wiring.
  </behavior>
  <action>Confirm and adjust hub rendering logic so newly expanded records appear in feature and industry hub card/link sets with deterministic ordering. Keep sitemap generation sourced from `solutionSlugs` and `industrySlugs` so expansion is automatically indexable without manual path edits.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All expansion pages are reachable from hubs and included in sitemap output through shared contract sources.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden expansion validation for link and intent safety</name>
  <files>lib/__tests__/cluster-content.test.ts, lib/__tests__/cluster-coverage.test.ts, scripts/seo/check-cluster-content.mjs</files>
  <behavior>
    - Expansion fails fast on intent overlap, missing reciprocity, or missing parent/sibling coverage.
    - Existing priority-trade coverage checks remain intact.
    - Validation output clearly identifies failing slugs and rule category.
  </behavior>
  <action>Extend contract and coverage tests plus script guardrails to explicitly validate expansion eligibility outcomes (intent uniqueness, reciprocal integrity, and CLUS-04 parent/sibling link guarantees). Preserve all existing Phase 2 checks; add expansion checks as incremental, deterministic assertions.</action>
  <verify>
    <automated>npx vitest run lib/__tests__/cluster-content.test.ts lib/__tests__/cluster-coverage.test.ts lib/__tests__/cluster-link-graph.test.ts && node scripts/seo/check-cluster-content.mjs</automated>
  </verify>
  <done>Expansion regressions for intent or link graph safety are blocked in both Vitest and script guardrails.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `npx vitest run lib/__tests__/cluster-content.test.ts lib/__tests__/cluster-coverage.test.ts lib/__tests__/cluster-link-graph.test.ts`
- [ ] `node scripts/seo/check-cluster-content.mjs`
</verification>

<success_criteria>
- Expanded feature/industry records satisfy eligibility rules before merge.
- Expanded pages preserve deterministic parent/sibling/cross-cluster crawl paths.
- Intent overlap and orphan regressions are blocked automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/03-link-graph-and-cluster-expansion/03-02-SUMMARY.md`
</output>
