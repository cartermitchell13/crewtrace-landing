---
phase: 03-link-graph-and-cluster-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cluster-link-graph.ts
  - app/features/[slug]/page.tsx
  - app/industries/[slug]/page.tsx
  - lib/__tests__/cluster-link-graph.test.ts
  - lib/__tests__/cluster-coverage.test.ts
  - scripts/seo/check-cluster-content.mjs
autonomous: true
requirements:
  - CLUS-04
must_haves:
  truths:
    - "Every indexable feature detail page exposes at least one sibling internal link and parent-path links."
    - "Every indexable industry detail page exposes at least one sibling internal link and parent-path links."
    - "Cross-cluster relationships are reciprocal and deterministic for all rendered related links."
  artifacts:
    - path: "lib/cluster-link-graph.ts"
      provides: "Deterministic sibling ranking and reciprocal graph utilities derived from typed contracts."
    - path: "app/features/[slug]/page.tsx"
      provides: "Feature detail page renders sibling + cross-cluster links from shared graph helpers."
    - path: "app/industries/[slug]/page.tsx"
      provides: "Industry detail page renders sibling + cross-cluster links from shared graph helpers."
    - path: "lib/__tests__/cluster-link-graph.test.ts"
      provides: "Unit-level regression checks for sibling ranking, tie-break, and reciprocity."
    - path: "scripts/seo/check-cluster-content.mjs"
      provides: "Script-level failure on orphan or reciprocity violations for indexable feature/industry pages."
  key_links:
    - from: "app/features/[slug]/page.tsx"
      to: "lib/cluster-link-graph.ts"
      via: "uses shared sibling/cross-cluster helper outputs"
      pattern: "from \"@/lib/cluster-link-graph\""
    - from: "app/industries/[slug]/page.tsx"
      to: "lib/cluster-link-graph.ts"
      via: "uses shared sibling/cross-cluster helper outputs"
      pattern: "from \"@/lib/cluster-link-graph\""
    - from: "scripts/seo/check-cluster-content.mjs"
      to: "lib/solutions.ts + lib/industries.ts"
      via: "enforces reciprocal and non-orphan graph invariants"
---

<objective>
Implement deterministic parent/sibling/internal-link graph rules across feature and industry clusters.

Purpose: Fulfill CLUS-04 by guaranteeing parent and sibling internal-link coverage on all indexable feature/industry pages.
Output: Shared link-graph helpers, detail-template integration, and blocking guardrails for orphan and reciprocity regressions.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-link-graph-and-cluster-expansion/03-CONTEXT.md
@.planning/phases/03-link-graph-and-cluster-expansion/03-RESEARCH.md
@.planning/phases/02-priority-cluster-build-features-industries/02-02-SUMMARY.md
@.planning/phases/02-priority-cluster-build-features-industries/02-03-SUMMARY.md
@lib/solutions.ts
@lib/industries.ts
@app/features/[slug]/page.tsx
@app/industries/[slug]/page.tsx
@scripts/seo/check-cluster-content.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build shared deterministic link-graph helpers from typed contracts</name>
  <files>lib/cluster-link-graph.ts, lib/__tests__/cluster-link-graph.test.ts</files>
  <behavior>
    - Sibling sets are derived by overlap of opposite-cluster relationships.
    - Sibling ordering is deterministic: overlap score desc, slug asc tie-break.
    - Reciprocal relationship checks detect one-way feature/industry links.
  </behavior>
  <action>Create `lib/cluster-link-graph.ts` with pure helpers for feature and industry sibling derivation, reciprocal-link checks, and parent/sibling availability checks per slug. Keep all inputs sourced from `featureRecords`/`industryRecords` contracts. Add `lib/__tests__/cluster-link-graph.test.ts` covering ranking, tie-break behavior, and reciprocity detection against representative fixtures from current cluster records.</action>
  <verify>
    <automated>npx vitest run lib/__tests__/cluster-link-graph.test.ts</automated>
  </verify>
  <done>Graph helper outputs are deterministic and tests fail when reciprocity or ranking guarantees are broken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire detail templates to shared sibling and cross-cluster graph outputs</name>
  <files>app/features/[slug]/page.tsx, app/industries/[slug]/page.tsx, lib/cluster-link-graph.ts</files>
  <behavior>
    - Feature and industry detail pages each render at least one sibling internal link where eligible.
    - Existing cross-cluster recommended links remain deterministic and contract-driven.
    - Link density stays bounded (3-5 sibling links per section).
  </behavior>
  <action>Refactor feature and industry detail pages to consume graph helper outputs for sibling surfaces rather than route-local selection logic. Preserve existing parent hub paths and cross-cluster recommendation sections while adding same-cluster sibling sections with deterministic ordering and bounded counts. Keep no route-local hardcoded relationship arrays.</action>
  <verify>
    <automated>npm run lint -- app/features/[slug]/page.tsx app/industries/[slug]/page.tsx lib/cluster-link-graph.ts && npm run build</automated>
  </verify>
  <done>Both detail templates compile and render deterministic sibling + cross-cluster links with parent-path continuity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend guardrails to fail on orphan and reciprocity regressions</name>
  <files>lib/__tests__/cluster-coverage.test.ts, scripts/seo/check-cluster-content.mjs</files>
  <behavior>
    - Tests and script checks fail if any indexable feature/industry page lacks parent or sibling coverage.
    - One-way cross-cluster relationships fail validation.
    - Error output names violating slugs for fast remediation.
  </behavior>
  <action>Expand `lib/__tests__/cluster-coverage.test.ts` and `scripts/seo/check-cluster-content.mjs` to enforce CLUS-04 parent/sibling coverage and bidirectional relationship integrity for all feature/industry records. Keep existing required-trade and intent checks intact; extend rather than replace guardrail behavior.</action>
  <verify>
    <automated>npx vitest run lib/__tests__/cluster-content.test.ts lib/__tests__/cluster-coverage.test.ts lib/__tests__/cluster-link-graph.test.ts && node scripts/seo/check-cluster-content.mjs</automated>
  </verify>
  <done>Automated checks block orphan and reciprocity regressions across all indexable feature/industry pages.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `npx vitest run lib/__tests__/cluster-content.test.ts lib/__tests__/cluster-coverage.test.ts lib/__tests__/cluster-link-graph.test.ts`
- [ ] `node scripts/seo/check-cluster-content.mjs`
</verification>

<success_criteria>
- Parent/sibling graph behavior is deterministic and centralized in shared helpers.
- Feature and industry detail routes expose parent + sibling + reciprocal cross-cluster links.
- CLUS-04 failure states are blocked by both test and script guardrails.
</success_criteria>

<output>
After completion, create `.planning/phases/03-link-graph-and-cluster-expansion/03-01-SUMMARY.md`
</output>
